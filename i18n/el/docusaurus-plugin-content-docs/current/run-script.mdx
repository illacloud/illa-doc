---
title: Εκτέλεση Σεναρίου
description: Με την ενεργοποίηση του Run Script μέσω ενός χειριστή γεγονότων, μπορείτε να επιτύχετε την διαδοχική εκτέλεση των λειτουργιών και να εκτελέσετε διαφορετικές ενέργειες με βάση διαφορετική κατάσταση.
---

# Πώς-να

<div style={{
    position: "relative",
    paddingBottom: "calc(56.81216931216932% + 41px)", 
    height: 0
    }}><iframe src="https://demo.arcade.software/r6dDc3eLjVfcrx2yGZnQ?embed" frameborder="0" loading="lazy" webkitallowfullscreen mozallowfullscreen allowfullscreen style={{position: "absolute", top: 0, left: 0, width: 100%, height: 100%, colorScheme: "light"}}></iframe></div>

## Επίδειξη

<div style={{
    position: "relative",
    paddingBottom: "calc(56.81216931216932% + 41px)", 
    height: 0
    }}><iframe src="https://builder.illacloud.com/illacloud_demo/deploy/app/ILAfx4p1C7Ac" frameborder="0" loading="lazy" webkitallowfullscreen mozallowfullscreen allowfullscreen style={{position: "absolute", top: 0, left: 0, width: 100%, height: 100%, colorScheme: "light"}}></iframe></div>

### Εκτέλεση και πιρούνι

https://illa.ai/app/ILAfx4p1C7Ac/λεπτομέρεια

## Περίπτωση χρήσης: Σύνδεση και ανακατεύθυνση σε διαφορετικές σελίδες

<div style={{
    position: "relative",
    paddingBottom: "calc(56.81216931216932% + 41px)", 
    height: 0
    }}><iframe src="https://builder.illacloud.com/illacloud_demo/deploy/app/ILAex4p1C7Eh" frameborder="0" loading="lazy" webkitallowfullscreen mozallowfullscreen allowfullscreen style={{position: "absolute", top: 0, left: 0, width: 100%, height: 100%, colorScheme: "light"}}></iframe></div>

### Εκτέλεση και πιρούνι

https://illa.ai/app/ILAex4p1C7Eh/detail

# ILLA custom functions

## Ενέργεια ενεργοποίησης

### Δομή

```JavaScript
const exampleData = αναμένει actionName.trigger();
```

Το όνομα ενέργειας είναι η συμβολοσειρά που εμφανίζεται στο στιγμιότυπο οθόνης, ως εξής.

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=NTQzYzllNmQ2NTdmNTM1ZJZjZDU3MTNjMjk3ZTI1ZDVfT2RwQTNkaEZSNHdpSmFOeVlmcVJPN2NaOXZ6N1N1NnBfVG9rZW46TGhSZWJEYW00b05xUDJ4OXDVYnVYOTNOc2RoXE2OTgzMDQ1MTI6MTY5ODMwExMl9WNA)

### Περιγραφή

Στην Εκτέλεση σεναρίου, η μεταβλητή `exampleData` είναι η ίδια με την `actionName.data`.

### Παράδειγμα

Για παράδειγμα, χρησιμοποιούμε τη δράση `postgresql1` για να ρωτήσουμε το email του James.

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=NzdlOTEyZjBm2M3MWJlZwYTIwZjkzMTQ1OTBhM2VfZEVNeTJRZGE2TFRObkNXTlV0OHdFOE85YzFYaU1JaVfVG9rZW46VU0yYWI4RHWbzQ2SWV4RkhBcXVcGNZc1NXZE2OTgzMDQ1MTI6MTY5ODMwODExMl9WNA)

Ενεργοποίηση της ενέργειας μέσω Εκτέλεσης Σεναρίου

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=YTQzOTE3NDI3YzU1MzVhOTY2ZTg4ODBjOWUyZmMxYzlfdW9uYzR5VXLYVNFMXRlYnpYU2xTTHo3WHJEZUpnaEJfVG9rZW46V1hVdGUWGdyb2FZOGh4R3BUQ3VjNk321cjXzE2OTgzMDQ1MTI6MTY5ODMwODMl9WNA)

`postgresql1.data` είναι

```JSON
[
  {
    "email": "James@outlook.com",
    "id": "1",
    "img": "https://images.pexels.com/photos/1222271/pexels-photo-1222271.jpeg?auto=compress&cs=tinysrgb&w=1600",
    "name": "James"
  }
]
```

When we need to use JavaScript to retrieve an email in the component's Inspect or elsewhere, we can use the following: `{{postgresql1.data[0].email}}`

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=ZDQyMWExZThhMzU5MGQwZTVlMDI0ZmUwNzk3NGYzZGJfdVdxNVFCS2hSMDY4YUtHWENpeE1LVFlFWmp0cHVtRzlfVG9rZW46RUpjTGJyZTI4b1dab3Q0JMN3VTDN4c3JoXzE2OTgzMDQ1MTI6MTY5ODExMwODExMl9WNA)

And when we need to use the email in the "Run Script" after executing the statement `const exampleData = await postgresql1.trigger();`, we can directly use `exampleData[0].email`.

Show a success notification when the query is successful and the result is unique. Show a failure notification when the query result is not unique or when it fails.

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=YjEyZDk4MjdjZjE5ZTdlZGQ0NzQ4NmI5ZDQ5MDE0ODBfTWhTdkI1VGI3Vk1FdG84WlNqYmtvNzVBVVmowRjRYbVZfVG9rZW46VmVwNmJLTkb3BmNDR4d3NSUHV3SmJOc2xmXE2OTgzMDQ1MTI6MTY5ODMwODMl9WNA)

```JSON
const exampleData = await postgresql1.trigger();
//show email via notification when the length of the resulting array is 1.
if(exampleData.length==1){
utils.showNotification(
  {
    type:"info",
    title:"Retrieved successfully",
    description:exampleData[0].email,
    duration: 4500}
);
  }else{
  //else, show a failed notification
utils.showNotification(
  {
    type:"info",
    title:"Failed to retrieve",
    duration: 4500
  }
);
  };
```

## Εμφάνιση ειδοποίησης

### Δομή

```JavaScript
utils.showNotification(
Ş
type: "info",
title: "Συγχαρητήρια",
description: "Retrieved successfully",
duration: 4500
}
)
```

### Περιγραφή

| Παράμετρος   | Περιγραφή                                                                   | Τύπος δεδομένων                                                                                 | Παράδειγμα                                   |
| ------------ | --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | -------------------------------------------- |
| τύπος        | Χρησιμοποιείται για τον ορισμό του τύπου εικονιδίου στην ειδοποίηση.        | Συμβολοσειρά, μία από τις ακόλουθες τιμές:"info"`default`"success""error""warning"              | τύπος: "πληροφορίες"                         |
| τίτλος       | Χρησιμοποιείται για τον ορισμό του τίτλου της ειδοποίησης                   | Συμβολοσειρά                                                                                    | title: "Συγχαρητήρια"                        |
| περιγραφή    | Χρησιμοποιείται για τον ορισμό της περιγραφής                               | Συμβολοσειρά                                                                                    | περιγραφή: "Ανάκτηση με επιτυχία"            |
| διάρκεια     | Χρησιμοποιείται για τον ορισμό της διάρκειας εμφάνισης της ειδοποίησης.     | Αριθμός, σε χιλιοστά του δευτερολέπτου (ms)                                                     | διάρκεια: 4500                               |

### Παράδειγμα

Εμφάνιση ειδοποίησης όταν κάνετε κλικ στο κουμπί:

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=NmNlZWMxNzFiMWVjZjJZDI0MjgzMzAwNGE2ODQzNjdfRFbXhKaTk2a2RIVFh2dnI0ZW5UcHVtZUEwZ3RHa0ZfVG9rZW46THZkYWJnYkFxb01HOHN4QTcycnVyRzBWc0tlXE2OTgzMDQ1MTI6MTY5ODMwODMl9WNA)

Είναι το ίδιο με την ενέργεια ειδοποίησης εμφάνισης, όπως δείχνει το ακόλουθο στιγμιότυπο οθόνης:

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=NTY2ZmNmMDQ2MjE5ZjM2NTkyNTZlMjZmYjk5YjA4ZTBfdExSYUZqTVVzMUtiZnBjaEVqZFRYSGdIcklLSmtMSXFfVG9rZW46Q21SdWJpb0tib0VpSDl4OG1oQnVuVGZrczdkXE2OTgzMDQ1MTI6MTY5ODExMl9WNA)

## Στοιχείο ελέγχου

### Δομή

```JavaScript
componentDisplayName.function()
```

componentDisplayName είναι η συμβολοσειρά που εμφανίζεται στο στιγμιότυπο οθόνης, ως εξής.

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=ODUwYzM1ZDVlMzFjYjhMThjZGZjNzExYmIOWI4NjVfdGtlbTRXS3ZNall1aTRzWJoZGtwVTl2VlRBa3VYYW5fVG9rZW46V3o5VWJUajV4b3IzUlp4VFMbHVpdEJmc3FnXE2OTgzMDQ1MTI6MTY5ODMwODExMl9WNA)

### Περιγραφή

#### ορισμόςΤιμή (παράμετρος)

Used to set the value. The parameter could be a `string` or `number` or `object` or `boolean` or `array` which depends on the component type.

##### Υποστηριζόμενα εξαρτήματα

| Συστατικό                                                                                                                                                                                         | Τύπος παραμέτρου                                                      | Παράδειγμα                                                                                                                                                                                     |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Είσοδος, Επεξεργάσιμο Κείμενο, Περιοχή Κειμένου, Διαχωριστικό, Επιλέξτε, Ομάδα Ραδιοφώνου, Κουμπί Radio Buttonm, Ημερομηνία, Χρόνος Ημερομηνίας, Βήματα, Κείμενο, Διάταξη Φραγμών, Πρόοδος Κύκλων | `string`                                                              | `input1.setValue("Hello world")`                                                                                                                                                               |
| JSON Editor                                                                                                                                                                                       | `string`                                                              | `jsonEditor1.setValue(`Ş"firstName": "Chuck", "lastName": "Norris", "age": 80, "bio": "Roundhouse kicking asses from 1940", "password": "noneed", "telephone": "1-800-KICKASS" }`)`            |
| Χρονοδιάγραμμα                                                                                                                                                                                    | `string`, Διαχωρίστε τις τιμές των διαφορετικών κόμβων μέσω κόμμα (,) | `timeline1.setValue("Το πρώτο ορόσημο,Το δεύτερο ορόσημο")`                                                                                                                                    |
| Είσοδος Αριθμού, Ρυθμιστικό, Ρυθμός                                                                                                                                                               | `number`                                                              | `numberInput1.setValue(1000)`                                                                                                                                                                  |
| Εναλλαγή                                                                                                                                                                                          | `boolean`                                                             | `switch1.setValue(false)`                                                                                                                                                                      |
| Μορφή Σχήματος Jun, Μορφή                                                                                                                                                                         | `object`                                                              | `jsonSchemaForm1.setValue(Ş"firstName": "Chuck", "lastName": "Norris", "age": 80, "bio": "Roundhouse κώλοι kicking κώλοι από το 1940", "password": "noneed", "telephone": "1-800-KICKASS" })`  |
| Ομάδα Checkbox, Cascader                                                                                                                                                                          | `array`                                                               | `cascader1.setValue(["media_source_2","campaign_2-1"])`                                                                                                                                        |

#### clearValue()

Χρησιμοποιείται για τον καθαρισμό της τιμής

##### Υποστηριζόμενα εξαρτήματα

Είσοδος, Αριθμός Εισόδου, Επεξεργάσιμο Κείμενο, Περιοχή Κειμένου, Μεταφόρτωση, Jsn Editor, Jsn Schema Form, Διακόπτης, Επιλογή, Πολλαπλών Επιλογών, Ομάδα Checkbox, Cascader, Ομάδα Ραδιοφώνου, Κουμπί Ραδιοφωνίας, Ημερομηνία, Χρονοδιάγραμμα, Χρόνος Δεδομένων, Ημερομηνία Ώρας, Κείμενο, Βαθμός, Bar Progress, Πρόοδος Κύκλων, Χρονοδιάγραμμα, Διαχωριστής

##### Παράδειγμα

```JavaScript
input1.clearValue()
```

#### εστίαση()

Χρησιμοποιείται για να επικεντρωθεί στο στοιχείο

##### Υποστηριζόμενα εξαρτήματα

Είσοδος, Αριθμός Είσοδος, Επεξεργάσιμο κείμενο, Περιοχή κειμένου, JSON Editor, Πλούσιο πρόγραμμα επεξεργασίας, Ομάδα Ραδιοφώνου,

##### Παράδειγμα

```JavaScript
input1.focus()
```

#### validate()

When you have set validation conditions for a component, such as email format, URL format, or required field, you can use the `validate()` function to determine if it meets the requirements. If it does not meet the requirements, a validation message will be displayed.

Ορίστε τους όρους επικύρωσης:

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=YjIwNTcyN2VjZDZkOTYzNGFlM0ZmVhZjYmI2MThfMUQ0ME5hZ0dOMEVsWGVEcHVxdm03cTdsVHBvaktOcTZfVG9rZW46QzhzemJoeXhkb1E1E1a0N4R0dQSnVDeUpZhiXzE2OTgzMDQ1MTI6MTY5ODMwODExMl9WNA)

Εμφάνιση του μηνύματος επικύρωσης:

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=OWQ1NzRmMTkxOGE3NjFjMTYzYTYxNGMzTE0N2QyY2ZfNEt4Q3ozbU1HRDJOYTVvaHNmYmpUZWRDMTZdmR0N3lfVG9rZW46SDhFOWJqdWU0b2tpbHd4UFVIVnU5M0hlczliXzE2OTgzMDQ1MTI6MTY5ODExMwODExMl9WNA)

##### Υποστηριζόμενα εξαρτήματα

Είσοδος, Αριθμός Είσοδος, Επεξεργάσιμο Κείμενο, Περιοχή Κειμένου, Μεταφόρτωση, Επιλογή, Πολλαπλών Επιλογών, Ομάδα Checkbox, Ομάδα Ραδιοφώνου, Κουμπί Ραδιοφωνίας, Ημερομηνία, Εύρος Ημερομηνίας, Ώρα Ημερομηνίας, Φόρμα, Ποσοστό

##### Παράδειγμα

```JavaScript
input1.validate()
```

#### clearValidation()

Χρησιμοποιείται για την εκκαθάριση του μηνύματος επικύρωσης.

##### Υποστηριζόμενα εξαρτήματα

Είσοδος, Αριθμός Είσοδος, Επεξεργάσιμο Κείμενο, Περιοχή Κειμένου, Μεταφόρτωση, Επιλέξτε, Πολλαπλών Επιλογών, Ομάδα Checkbot, Κουμπί Ραδιοφωνίας, Ημερομηνία, Χρονικό Εύρος Ημερομηνίας, Ώρα Ημερομηνίας, Τιμή

##### Παράδειγμα

```JavaScript
input1.clearValidation()
```

#### setSelectedValue(πίνακας)

Used to set the selected value of Multiselect. The parameter should be an array.

##### Υποστηριζόμενα εξαρτήματα

Πολλαπλής επιλογής

##### Παράδειγμα

```JavaScript
multiselect1.setSelectedValue(["Επιλογή 1", "Επιλογή 3"])
```

#### υποβολή()

Submit form data. When the submit method is triggered, we will first validate the form data. If there are validation errors, the form submission will be prevented, and the corresponding error messages will be displayed. Only after successful validation does the submission proceed.

##### Υποστηριζόμενα εξαρτήματα

Μορφή Σχήματος Jun, Μορφή

##### Παράδειγμα

```JavaScript
form1.submit()
```

#### εναλλαγή()

Χρησιμοποιείται για να αλλάξετε την τιμή από true σε false ή από false σε true.

##### Υποστηριζόμενα εξαρτήματα

Εναλλαγή

##### Παράδειγμα

```JavaScript
switch1.toggle()
```

#### setStartOfRange(number)

Χρησιμοποιείται για τον ορισμό της προεπιλεγμένης τιμής έναρξης του ρυθμιστικού εύρους

##### Υποστηριζόμενα εξαρτήματα

Range Slider

##### Παράδειγμα

```JavaScript
rangeSlider1.setStartOfRange(1)
```

#### setEndOfRange(number)

Χρησιμοποιείται για τον ορισμό της προκαθορισμένης τελικής τιμής του ρυθμιστικού εύρους

##### Υποστηριζόμενα εξαρτήματα

Range Slider

##### Παράδειγμα

```JavaScript
rangeSlider1.setEndOfRange(9)
```

#### setStartValue(string)

Χρησιμοποιείται για τον ορισμό της προεπιλεγμένης τιμής έναρξης

##### Υποστηριζόμενα εξαρτήματα

Εύρος Ημερομηνίας, Χρονικό Εύρος

##### Παράδειγμα

```JavaScript
dateRange1. setStartValue ("2023- 09- 09")
```

#### setEndValue(string)

Χρησιμοποιείται για τον ορισμό της προκαθορισμένης τελικής τιμής

##### Υποστηριζόμενα εξαρτήματα

Εύρος Ημερομηνίας, Χρονικό Εύρος

##### Παράδειγμα

```JavaScript
dateRange1. setEndValue("2023-10- 09")
```

#### setValueInArray(πίνακας)

Χρησιμοποιείται για τον ορισμό των διακοπτών των οποίων η τιμή είναι αληθής

##### Υποστηριζόμενα εξαρτήματα

Εναλλαγή Ομάδας

##### Παράδειγμα

```JavaScript
switchGroup1.setValueInArray(["Επιλογή 1", "Επιλογή 3"])
```

#### selectPage(number)

Used to set the index of the page to display. The index is a number starting from ０.

##### Υποστηριζόμενα εξαρτήματα

Πίνακας

##### Παράδειγμα

```JavaScript
table1.selectPage(1)
```

#### selectRow(number)

Used to set the index of selected row. The index is a number starting from ０.

##### Υποστηριζόμενα εξαρτήματα

Πίνακας

##### Παράδειγμα

```JavaScript
table1.selectRow(1)
```

#### clearSelection()

Χρησιμοποιείται για τον καθαρισμό της επιλεγμένης γραμμής.

##### Υποστηριζόμενα εξαρτήματα

Πίνακας

##### Παράδειγμα

```JavaScript
table1.clearSelection()
```

#### setFilters(πίνακας αντικειμένων, συμβολοσειρά)

Χρησιμοποιείται για τον ορισμό φίλτρων.

**The first parameter** is an array of objects. Each object represents a filtering condition. It includes three attributes:

**`id`** : This refers to the id of this column. If this column is a result of a database query, then the id would be the column name you retrieved from the query. For example, if the query is `SELECT email AS user_email FROM users` and rename `email` as `Email` using the right-hand panel of the table, the id of the column would still be `user_email` If this column is manually added using the `Add Column` button in the right-hand panel, you can check its id in the `displayedData` section of the left-hand panel. Typically, it will be a randomly generated string starting with `column` such as `column-320169e8-225a-482d-9853-ad34b3040220`.

**`filterFn`** : the match type of the filter. We support the following types: `equalTo` which is the same as `=` in SQL, `notEqualTo` which is the same as `<>` in SQL, `contains` which is the same as `LIKE '%value%'` in SQL, `does NotContain` which is the same as `NOT LIKE '%value%'` in SQL, `lessThan` which is the same as `<` in SQL, `notLessThan` which is the same as `>=` in SQL, `notMoreThan` which is the same as `<=` in SQL, `empty` which is the same as `IS NULL` in SQL, `notEmpty` which is the same as `IS NOT NULL` in SQL, `before` is used to compare time or date, which is the same as `>` in SQL, `after` is used to compare time or date, which is the same as `<` in SQL.

**\<3>** **`value`** : χρησιμοποιείται για τον ορισμό της τιμής προς σύγκριση.

**The second parameter:** when there are multiple filter condition objects, it determines whether they have an "AND" relationship or an "OR" relationship. Set it as a string `and` for an "AND" relationship, and `or` for an "OR" relationship.

##### Υποστηριζόμενα εξαρτήματα

Πίνακας

##### Παράδειγμα

```JavaScript
table1.setFilters([{"id":"month", "filterFn":"equalTo", "value":"May"}, {"id":"μήνας", "filterFn":"equalTo", "value":"Ιούνιο"}], "ή")
```

#### clearFilters()

Χρησιμοποιείται για καθαρισμό όλων των φίλτρων

##### Υποστηριζόμενα εξαρτήματα

Πίνακας

##### Παράδειγμα

```JavaScript
table1.clearFilters()
```

#### setSort(string, string)

Used to set the sort. **The first parameter** is the id of the column to sort which is a string value. The id is the same as the id mentioned in setFilters(array of objects, string). **The second parameter** is the sorting order, either ascending or descending. It is set using a string value `ascend` or `descend`.

##### Υποστηριζόμενα εξαρτήματα

Πίνακας

##### Παράδειγμα

```JavaScript
table1.setSort("εισοδήματα", "φθίνουσα")
```

#### setCurrentViewKey(string)

Χρησιμοποιήστε το πλήκτρο που είναι μια συμβολοσειρά της προβολής για να ορίσετε την προβολή που εμφανίζεται.

##### Υποστηριζόμενα εξαρτήματα

Περιέκτης

##### Παράδειγμα

```JavaScript
container1. setCurrentViewKey("Προβολή 2")
```

#### setCurrentViewIndex(αριθμός)

Χρησιμοποιήστε το ευρετήριο που είναι μια τιμή αριθμού ξεκινώντας από 0 της προβολής για να ορίσετε την προβολή που εμφανίζεται.

##### Υποστηριζόμενα εξαρτήματα

Περιέκτης

##### Παράδειγμα

```JavaScript
container1.setCurrentViewIndex(1)
```

#### showNextView(boolean)

Used to show the next view. The parameter is a boolean value that determines whether to loop from the end back to the start.

##### Υποστηριζόμενα εξαρτήματα

Περιέκτης

##### Παράδειγμα

```JavaScript
container1.showNextView(true)
```

#### showNextVisibleView(boolean)

Used to show the next visible view. Unlike the behavior of the `showNextView(boolean)` function, this function offers improved view switching functionality by skipping any hidden views during the transition. The parameter is a boolean value that determines whether to loop from the end back to the start.

##### Υποστηριζόμενα εξαρτήματα

Περιέκτης

##### Παράδειγμα

```JavaScript
container1.showNextVisibleView(false)
```

#### Προβολή()

Used to show the previous view. The parameter is a boolean value that determines whether to loop from the start back to the end.

##### Υποστηριζόμενα εξαρτήματα

Περιέκτης

##### Παράδειγμα

```JavaScript
container1.showPreviousView(true)
```

#### ΕμφάνισηΠροβολή()

Used to show the previous visible view. Unlike the behavior of the `showPreviousView(boolean)` function, this function offers improved view switching functionality by skipping any hidden views during the transition. The parameter is a boolean value that determines whether to loop from the start back to the end.

##### Υποστηριζόμενα εξαρτήματα

Περιέκτης

##### Παράδειγμα

```JavaScript
container1.showPreviousVisibleView(true)
```

#### reset()

Χρησιμοποιείται για να επαναφέρετε την τιμή στην προεπιλεγμένη τιμή.

##### Υποστηριζόμενα εξαρτήματα

Φόρμα

##### Παράδειγμα

```JavaScript
form1.reset()
```

#### resetValue()

Χρησιμοποιείται για να επαναφέρετε την τιμή στην προεπιλεγμένη τιμή.

##### Υποστηριζόμενα εξαρτήματα

Βήματα

##### Παράδειγμα

```JavaScript
steps1.resetValue()
```

#### openModal()

Χρησιμοποιείται για να ανοίξει ένα modal

##### Υποστηριζόμενα εξαρτήματα

Μοντάλ

##### Παράδειγμα

```JavaScript
modal1.openModal()
```

#### closeModal()

Χρησιμοποιείται για να κλείσει ένα modal

##### Υποστηριζόμενα εξαρτήματα

Μοντάλ

##### Παράδειγμα

```JavaScript
modal1.closeModal()
```

#### ορισμόςImageUrl(συμβολοσειρά)

Used to set the Image source URL. It can be a Base64 or a URL pointing to an image on the web.

##### Υποστηριζόμενα εξαρτήματα

Εικόνα

##### Παράδειγμα

```JavaScript
image1.setImageUrl("https://cloud-api.illacloud.com/drive/f/83095b77-1180-487e-86db-ff117e6083e5")
```

```JavaScript
image1. setImageUrl("δεδομένα: εικόνα/png, base64, iVBORw0KGgoAA...II=")
```

#### clearReplyMessage()

Χρησιμοποιείται για να καθαρίσετε το μήνυμα απάντησης που αναφέρεται πάνω από το πλαίσιο εισαγωγής.

##### Υποστηριζόμενα εξαρτήματα

Συνομιλία

##### Παράδειγμα

```JavaScript
chat1.clearReplyMessage()
```

#### setPrimaryValue(number)

Χρησιμοποιείται για τον ορισμό της κύριας αξίας των στατιστικών.

##### Υποστηριζόμενα εξαρτήματα

Στατιστικά

##### Παράδειγμα

```JavaScript
statistic1.setPrimaryValue(100)
```

#### resetPrimaryValue()

Χρησιμοποιείται για την επαναφορά της κύριας τιμής στην προεπιλεγμένη κύρια τιμή των στατιστικών.

##### Υποστηριζόμενα εξαρτήματα

Στατιστικά

##### Παράδειγμα

```JavaScript
statistic1.resetPrimaryValue()
```

#### setFileUrl(string)

Used to set the file URL. It can be a Base64 or a URL pointing to a PDF on the web.

##### Υποστηριζόμενα εξαρτήματα

PDF

##### Παράδειγμα

```JavaScript
pdf1.setFileUrl("https://upload.wikimedia.org/wikipedia/commons/e/ee/Guideline_No._GD-Ed-2214_Marman_Clamp_Systems_Design_Guidelines.pdf")
```

#### αναπαραγωγή()

Χρησιμοποιείται για την αναπαραγωγή βίντεο ή ήχου.

##### Υποστηριζόμενα εξαρτήματα

Βίντεο, Ήχος

##### Παράδειγμα

```JavaScript
video1.play()
```

#### παύση()

Χρησιμοποιείται για παύση βίντεο ή ήχου.

##### Υποστηριζόμενα εξαρτήματα

Βίντεο, Ήχος

##### Παράδειγμα

```JavaScript
video1.pause()
```

#### showControls(boolean)

Χρησιμοποιείται για να ορίσετε αν θα εμφανίζονται τα χειριστήρια.

##### Υποστηριζόμενα εξαρτήματα

Βίντεο

##### Παράδειγμα

```JavaScript
video1.showControls(true)
```

#### ορισμόςVideoUrl(συμβολοσειρά)

Used to set the video's URL. It can be a Base64 or a video URL on media websites.

##### Υποστηριζόμενα εξαρτήματα

Βίντεο

##### Παράδειγμα

```JavaScript
video1.setVideoUrl ("https://youtu.be/8sUovZlBh_M")
```

#### ορισμόςΈνταση(αριθμός)

Used to set the volume. The parameter is a number value between 0 to 1.

##### Υποστηριζόμενα εξαρτήματα

Βίντεο, Ήχος

##### Παράδειγμα

`video1.setVolume(0.5)`

#### ορισμόςΤαχύτητα(αριθμός)

Used to set the speed. The parameter is a number value between 0.25 to 2.

##### Υποστηριζόμενα εξαρτήματα

Βίντεο, Ήχος

##### Παράδειγμα

```JavaScript
video1.setSpeed(0.5)
```

#### setLoop(boolean)

Χρησιμοποιείται για να οριστεί αν θα επαναληφθεί η αναπαραγωγή.

##### Υποστηριζόμενα εξαρτήματα

Βίντεο, Ήχος

##### Παράδειγμα

```JavaScript
video1.setLoop(false)
```

#### seekTo( αριθμός)

Used to seek to a specific time position. The parameter is a number value, measured in seconds (s). For example, to seek to the time position 5:35, you would use the value 335.

##### Υποστηριζόμενα εξαρτήματα

Βίντεο, Ήχος

##### Παράδειγμα

```JavaScript
video1.seekTo(335)
```

#### mute(boolean)

Χρησιμοποιείται για τον ορισμό σίγασης ή όχι.

##### Υποστηριζόμενα εξαρτήματα

Βίντεο, Ήχος

##### Παράδειγμα

```JavaScript
video1.mute(true)
```

#### setAudioUrl(string)

Used to set the audio's URL. It can be a Base64 or a video URL on media websites.

##### Υποστηριζόμενα εξαρτήματα

Ήχος

##### Παράδειγμα

```JavaScript
video1.setAudioUrl("https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3")
```

#### slickNext()

Χρησιμοποιείται για εναλλαγή στην επόμενη εικόνα.

##### Υποστηριζόμενα εξαρτήματα

Carousel

##### Παράδειγμα

```JavaScript
carousel1.slickNext()
```

#### Προηγούμενος()

Χρησιμοποιείται για εναλλαγή στην προηγούμενη εικόνα.

##### Υποστηριζόμενα εξαρτήματα

Carousel

##### Παράδειγμα

```JavaScript
carousel1.slickΠροηγούμενος()
```

#### setSrc()

Χρησιμοποιείται για τον ορισμό της πηγής URL του IFrame.

##### Υποστηριζόμενα εξαρτήματα

IFrame

##### Παράδειγμα

```JavaScript
iframe1.setSrc ("https://www.nasa.gov/")
```

#### setMarkers(πίνακας)

Used to set the markers on the map. The parameter should be an array of objects. The latitude and longitude fields in each object should be same as the fields set in the right-hand panel

##### Υποστηριζόμενα εξαρτήματα

Χάρτης

##### Παράδειγμα

```JavaScript
map1.setMarkers([
{
    "lat":57.14009563040699,
    "lng":-114.6018657894011
 },{
     "lat":55.40432513713809,
     "lng":-112.98252263795787
 },{
     "lat":57.841288754888545,
     "lng":-103.351865787894011
 }
])
```

#### resetMarkers()

Χρησιμοποιείται για να επαναφέρετε τους δείκτες στους προεπιλεγμένους δείκτες.

##### Υποστηριζόμενα εξαρτήματα

Χάρτης

##### Παράδειγμα

```JavaScript
map1.resetMarkers()
```

## Μετάβαση στο URL

### Δομή

```JavaScript
utils.goToURL({
url:string,
newTab:boolean
})
```

### Περιγραφή

Use an object to set it. There are 2 attributes in the object. The first attribute is `url`. Its value is a complete URL, including 'https://' or 'http://', and it is of type string. The second attribute is `newTab`. It is a boolean used to set whether to open the URL in a new tab.

### Παράδειγμα

```JavaScript
utils.goToURL(ņ
url:"https://www.illacloud.com",
newTab:true
})
```

## Αντιγραφή στο πρόχειρο

### Δομή

```JavaScript
utils.copyToClipboard(string)
```

### Περιγραφή

Χρησιμοποιείται για αντιγραφή συμβολοσειράς

### Παράδειγμα

```JavaScript
utils.copyToClipboard("Το κείμενο προς αντιγραφή")
```

## Ορισμός δρομολογητή

### Δομή

```JavaScript
utils.setRouter({
pagePath:string;
viewPath:string
})
```

### Περιγραφή

Used to change the page in ILLA Apps. There are 2 attributes in the object.

The first attribute is `pagePath`. The value of pagePath is shown on the following screenshot without `/`.

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=ZTkwODAzZmEzYmNlMDhlZDlmYjYzMDA4NjFlYWIyMGJfWHE5WUtsWkplZnF1d05FaVuUW5IcnN6cVRtbkNWcjdfVG9rZW46RjZUamI1a084b0djVW54NWE2UnVZndTc21mXzE2OTgzMDQ1MTI6MTY5ODExMl9WNA)

The second attribute is `viewPath`. The value of viewPath is shown on the following screenshot without `/`.

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=MzljM2ZkMDExYmE1NmRmNTQwNDZjNmU2Nm4YmQ1NzBfRTdCUFd2T3MwOXFOVGpHNkxsUHZwdzFib29yMmJtVXpfVG9rZW46Vjl4d2JxdDB6b1lIazh4eElVOXUzMTRrc1JnXzE2OTgzMDQ1MTI6MTI6MTY5ODMwODMl9WNA)

### Παράδειγμα

```JavaScript
utils.setRouter(mptom
pagePath:"page2", 
viewPath:"sub-page1"
})
```

## Αποθήκευση στο δίσκο ILLA

### Δομή

```JavaScript
utils.saveToILLADrive(ņ
fileName:string,
fileData:string,
fileType:string,
folder:string,
allowAnonymous:boolean,
replace:boolean
})
```

### Περιγραφή

Χρησιμοποιείται για την αποθήκευση ενός αρχείου στο δίσκο ILLA.

`fileName`: Όνομα με επέκταση του αρχείου μετά το ανέβασμα στον οδηγό ILLA.

`fileData`: Base64 ή ακατέργαστα δεδομένα αρχείων, όπως JSON, CSV, απλό κείμενο, κλπ.

`fileType`: One of the following values: Auto, Plain text, JPEG, PNG, SVG, JSON, CSV, TSV, Excel(.xlsx). When using "Auto," we will attempt to determine the file type based on either the file extension or the file data.

`folder`: Destination folder for file upload. Leave it blank to upload to the root folder.

`allowAnonymous`: Used to set whether anonymous user access is allowed. If you plan to set your application as public, anonymous access must be enabled. For the security of your data, files or folders accessible anonymously must be placed in the anonymous folder.

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=Y2U5MzRmOGMxYWE4YzI2MjQxMjZhOWQ3ODFjNDNkMjlfTDJkSzg0QktPUmlQa3VZcDUxYXBsZVZVHVHVhazlXS1ZfVG9rZW46SnIyQ2JuaGpsb0s0dGJ4Z1libnVkSjZLc0lmXzE2OTgzMDQ1MTI6MTY5ODMwODML9WNA)

`replace`: Χρησιμοποιείται για να ορίσετε όταν υπάρχουν αρχεία με το ίδιο όνομα στον ίδιο κατάλογο, αντικατάσταση του υπάρχοντος αρχείου με νέο αρχείο ή αυτόματη μετονομασία του νέου αρχείου.

### Παράδειγμα

```JavaScript
//use upload1 to pick files from device and upload to apps folder. 
utils.saveToILLADrive(⇒
fileName:upload1.files[0].name,
fileData:upload1. iles[0].dataURI,
fileType:"PNG",
φάκελος:"apps",
allowAnonymous:false,
replace:true
})
```

```JavaScript
//upload ένα αρχείο κειμένου.  
utils.saveToILLADrive(Ş
fileName:"test.txt",
fileData:"abc",
fileType:"Plain text",
φάκελο:"apps",
allowΑνώνυμα:false,
αντικατάσταση:true
})
```

## Λήψη από το δίσκο ILLA

### Δομή

```JavaScript
utils.downloadFromILLADrive({
    downloadInfo:[{
        tinyURL:string,
        fileID:string
        }],
    asZip:boolean
})
```

### Περιγραφή

Χρησιμοποιείται για τη λήψη αρχείων από το ILLA Drive.

`downloadInfo`: an array of objects including tinyURL and fileID. You can use the component Drive file picker to pick files from ILLA Drive and get the `downloadInfo` via drivePicker1.value

`asZip`: χρησιμοποιείται για να ορίσετε αν θα γίνει λήψη των αρχείων ως Zip.

### Παράδειγμα

```JavaScript
utils.downloadFromILLADrive({
    downloadInfo:drivePicker1.value,
    asZip:true
})
```

## Λήψη

### Δομή

```JavaScript
utils.downloadFile({
    fileType:string,
    fileName:string,
    data:anyType
})
```

### Περιγραφή

Χρησιμοποιείται για εξαγωγή δεδομένων στη συσκευή σας.

`fileType`: One of the following values: Auto, Plain text, JPEG, PNG, SVG, JSON, CSV, TSV, Excel(.xlsx). When using "Auto," we will attempt to determine the file type based on either the file extension or the file data.

`fileName`: ένα όνομα με επέκταση.

`data`: δεδομένα του αρχείου.

### Παράδειγμα

```JavaScript
//κατεβάστε τα δεδομένα του πίνακα ως CSV
utils.downloadFile(Ş
    fileType:"CSV",
    fileName:"text.csv",
    data: table2.dataSourceJS
})
```

```JavaScript
//download image
utils.downloadFile(ņ
    fileType:"PNG",
    fileName:"text.png",
    data: image1.imageSrc
})
```

## Ορισμός καθολικών δεδομένων

### Ορισμός καθολικής τιμής δεδομένων

#### Δομή

```JavaScript
utils.setGlobalDataValue({
key:string, 
value:any
})
```

#### Περιγραφή

Χρησιμοποιείται για τον ορισμό της τιμής gobalData

`key`: η οθόνηΌνομα του globalData όπως δείχνει το ακόλουθο στιγμιότυπο οθόνης.

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=NGE2OTMxZGFlYjE1ZmYwMGZlOWQ3MWE2ZDFiNTQ3ZmFMDRUUWxYN3JxNdSZXdFdzI2blFZeDNUVFozRnRCYnVfVG9rZW46TUc0Ym28wbzhXdDl4aEJ4YnVRBC21jXzE2OTgzMDQ1MTI6MTY5ODMwODMl9WNA)

`value`: η τιμή που θα οριστεί

#### Παράδειγμα

```JavaScript
//use input5 to get the user input value and set as the value of stringState. 
utils.setGlobalDataValue({
key:"stringState", 
value:input5.value
})
```

### Ορισμός καθολικών δεδομένων σε

#### Δομή

```JavaScript
utils.setGlobalDataIn({
key:string, 
path:string, 
value:any
})
```

#### Περιγραφή

Όταν τα καθολικά δεδομένα είναι ένας πίνακας ή ένα αντικείμενο, Χρησιμοποιήστε το setGlobalDataΓια να τροποποιήσετε την τιμή που αντιστοιχεί σε ένα συγκεκριμένο ευρετήριο σε έναν πίνακα ή για να τροποποιήσετε την τιμή που αντιστοιχεί σε ένα συγκεκριμένο κλειδί σε ένα αντικείμενο.

`key`: η οθόνηΌνομα του globalData.

`path`:  the index of the value you intend to update in the array or the key in the object. For example, to set the second value in array, the path is 1 (the index). To set the value of the attribute named key1, the path is key1.

`value`: η τιμή που θα οριστεί

#### Παράδειγμα

```JavaScript
//objectState είναι ένα αντικείμενο: {"key1":"value1", "key2":"value2"}, to set the 
utils.setGlobalDataIn({
key:"objectState", 
path:"key2", 
value:input7.value
})
```

# Ενσωματωμένες βιβλιοθήκες

| **Βιβλιοθήκη**    | **Έγγραφο**                                                           | **Πώς να το χρησιμοποιήσετε σε ILLA Builder** | **Παράδειγμα**                                               |
| ----------------- | --------------------------------------------------------------------- | --------------------------------------------- | ------------------------------------------------------------ |
| Lodash            | https://lodash.com/docs/4.17.15                                       | `_`                                           | `_.concat(array, 2, [3], [[4]]);`                            |
| Day.js            | https://day.js.org/                                                   | `dayjs`                                       | `dayjs(new Date())`                                          |
| uuid              | <https://www.npmjs.com/package/uuid>                                  | `uuid`                                        | `uuid.parse()`                                               |
| numbro            | https://numbrojs.com/                                                 | `numbro`                                      | `numbro(1000).format({thousandSeparated: true})`             |
| Papa              | https://www.papaparse.com/docs                                        | `Papa`                                        | `Papa.parse(url, {download: true, // rest of config ... })`  |

# Τιμή των ενεργειών, συστατικά...

Use `displayName.xxx` to use the value of actions, components, or other globalData. Please note that in the Run Script, these variables can be directly accessed without using `{{` references.

```JavaScript
utils.downloadFromILLADrive({
    downloadInfo:{{drivePicker1.value}},//❌ 
    asZip:true
})

utils.downloadFromILLADrive({
    downloadInfo:drivePicker1.value,//:check_mark_button:
    asZip:true
})
```
