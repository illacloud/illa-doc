---
title: Skript ausführen
description: Durch das Auslösen des Skripts durch einen Ereignis-Handler können Sie eine sequentielle Ausführung von Operationen und verschiedene Aktionen auf der Grundlage unterschiedlicher Bedingungen erreichen.
---

# Skript ausführen

Durch das Auslösen des Skripts durch einen Ereignis-Handler können Sie eine sequentielle Ausführung von Operationen und verschiedene Aktionen auf der Grundlage unterschiedlicher Bedingungen erreichen.

## How-to

<div style={{
    position: "relative",
    paddingBottom: "calc(56.81216931216932% + 41px)", 
    height: 0
    }}><iframe src="https://demo.arcade.software/r6dDc3eLjVfcrx2yGZnQ?embed" frameborder="0" loading="lazy" webkitallowfullscreen mozallowfullscreen allowfullscreen style={{position: "absolute", top: 0, left: 0, width: "100%", height: "100%", colorScheme: "light"}}></iframe></div>

### Demo

<div style={{
    position: "relative",
    paddingBottom: "calc(56.81216931216932% + 41px)", 
    height: 0
    }}><iframe src="https://builder.illacloud.com/illacloud_demo/deploy/app/ILAfx4p1C7Ac" frameborder="1" loading="lazy" webkitallowfullscreen mozallowfullscreen allowfullscreen style={{position: "absolute", top: 0, left: 0, width: "100%", height: "100%", colorScheme: "light"}}></iframe></div>

#### Ausführen und forken

https://illa.ai/app/ILAfx4p1C7Ac/Detail

### Anwendungsfall: Login und Weiterleitung zu verschiedenen Seiten

<div style={{
    position: "relative",
    paddingBottom: "calc(56.81216931216932% + 41px)", 
    height: 0
    }}><iframe src="https://builder.illacloud.com/illacloud_demo/deploy/app/ILAex4p1C7Eh" frameborder="1" loading="lazy" webkitallowfullscreen mozallowfullscreen allowfullscreen style={{position: "absolute", top: 0, left: 0, width: "100%", height: "100%", colorScheme: "light"}}></iframe></div>

#### Ausführen und forken

https://illa.ai/app/ILAex4p1C7Eh/Detail

## ILLA custom functions

### Aktion auslösen

#### Struktur

```JavaScript
const beispielData = warte actionName.trigger();
```

Der ActionName ist der im Screenshot angezeigte String.

![1](https://cdn.illacloud.com/official-website/img/docs/new-doc/run-script/1.png)

#### Beschreibung

Im Run Script ist die Variable `exampleData` die gleiche wie `actionName.data`.

#### Beispiel

Zum Beispiel verwenden wir die Aktion `postgresql1`, um James-E-Mails abzufragen.

![2](https://cdn.illacloud.com/official-website/img/docs/new-doc/run-script/2.png)

Aktion über Skript ausführen auslösen

![3](https://cdn.illacloud.com/official-website/img/docs/new-doc/run-script/3.png)

`postgresql1.data` ist

```JSON
[
  {
    "email": "James@outlook.com",
    "id": "1",
    "img": "https://images.pexels.com/photos/1222271/pexels-photo-1222271.jpeg?auto=compress&cs=tinysrgb&w=1600",
    "name": "James"
  }
]
```

Wenn wir JavaScript verwenden müssen, um eine E-Mail in der Komponenteninspektion oder anderswo abzurufen, können wir Folgendes verwenden: `{{postgresql1.data[0].email}}`

![4](https://cdn.illacloud.com/official-website/img/docs/new-doc/run-script/4.png)

Und wenn wir die E-Mail im "Skript ausführen" nach der Ausführung der Anweisung "const exampleData = warten postgresql1. rigger();`, wir können direkt `exampleData[0].email\` verwenden.

Erfolgreiche Benachrichtigung anzeigen, wenn die Abfrage erfolgreich ist und das Ergebnis eindeutig ist. Fehlerbenachrichtigung anzeigen, wenn das Abfrageergebnis nicht eindeutig ist oder wenn es fehlschlägt.

![5](https://cdn.illacloud.com/official-website/img/docs/new-doc/run-script/5.png)

```JSON
const exampleData = warten postgresql1.trigger();
//E-Mail per Benachrichtigung anzeigen, wenn die Länge des resultierenden Arrays 1 ist.
if(exampleData.length==1){
utils. howNotification(
  {
    type:"info",
    title:"Erfolgreich abgerufen",
    Beschreibung:exampleData[0]. mail,
    Dauer: 4500}
);
  }else{
  //sonst, zeige eine fehlgeschlagene Benachrichtigung
Utils. howNotification(
  {
    type:"info",
    title:"Fehler beim Abrufen",
    Dauer: 4500
  }
);
};
```

### Benachrichtigung anzeigen

#### Struktur

```JavaScript
utils.showNotification(
{
type: "info",
title: "Glückwunsch",
description: "Erfolgreich abgerufen",
Dauer: 4500
}
)
```

#### Beschreibung

| Parameter    | Beschreibung                                                                 | Datentyp                                                                          | Beispiel                                    |
| ------------ | ---------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ------------------------------------------- |
| typ          | Wird verwendet, um den Symboltyp in der Benachrichtigung festzulegen.        | String, einer der folgenden Werte: "info"`default`"success""error""warning"       | typ: "info"                                 |
| titel        | Verwendet um den Titel der Benachrichtigung zu setzen                        | String                                                                            | title: "Herzlichen Glückwunsch"             |
| beschreibung | Verwendet um die Beschreibung zu setzen                                      | String                                                                            | Beschreibung: "Erfolgreich abgerufen"       |
| dauern       | Wird verwendet, um die Anzeigedauer der Benachrichtigung festzulegen.        | Nummer, in Millisekunden (ms)                                                     | Dauer: 4500                                 |

#### Beispiel

Zeige Benachrichtigung, wenn du auf den Button klickst:

![6](https://cdn.illacloud.com/official-website/img/docs/new-doc/run-script/6.png)

Es ist das gleiche wie die Benachrichtigungsaktion anzeigen, wie der folgende Screenshot zeigt:

![7](https://cdn.illacloud.com/official-website/img/docs/new-doc/run-script/7.png)

### Komponente steuern

#### Struktur

```JavaScript
componentDisplayName.function()
```

componentDisplayName ist der im Screenshot angezeigte String.

![8](https://cdn.illacloud.com/official-website/img/docs/new-doc/run-script/8.png)

#### Beschreibung

##### setWert (Parameter)

Wird zum Setzen des Wertes verwendet. Der Parameter kann ein `string` oder `number` oder `object` oder `boolean` oder `array` sein, was vom Komponententyp abhängt.

###### Unterstützte Komponenten

| Komponente                                                                                                                                                                      | Parametertyp                                                     | Beispiel                                                                                                                                                                                     |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Eingabe, editierbarer Text, Textbereich, Divider, Auswahl, Radio-Gruppe, Radio-Schaltfläche, Radio-Buttonm, Datum, Uhrzeit, Schritte, Text, Balkenfortschritt, Kreisfortschritt | 'string'                                                         | `input1.setValue("Hallo Welt")`                                                                                                                                                              |
| JSON Editor                                                                                                                                                                     | 'string'                                                         | jsonEditor1.setValue(`{ "firstName": "Chuck", "lastName": "Norris", "age": 80, "bio": "Rundhaus kicking asses since 1940", "password": "noneed", "telephone": "1-800-KICKASS" }`)            |
| Zeitleiste                                                                                                                                                                      | `string`, Trenne die Werte verschiedener Knoten über Komma (,)   | `timeline1.setValue("Der erste Meilenstein,Der zweite Meilenstein")`                                                                                                                         |
| Nummerneingabe, Slider, Rate                                                                                                                                                    | `Nummer`                                                         | `numberInput1.setValue(1000)`                                                                                                                                                                |
| Wechseln                                                                                                                                                                        | `boolean`                                                        | `switch1.setValue(false)`                                                                                                                                                                    |
| JSON-Schema-Formular, Formular                                                                                                                                                  | `object`                                                         | `jsonSchemaForm1.setValue({ "firstName": "Chuck", "lastName": "Norris", "age": 80, "bio": "Roundhouse kicking asses since 1940", "password": "noneed", "telephon": "1-800-KICKASS" })`       |
| Checkbox-Gruppe, Cascader                                                                                                                                                       | 'array'                                                          | `cascader1.setValue(["media_source_2","campaign_2-1"])`                                                                                                                                      |

##### clearValue()

Wird zum Löschen des Wertes verwendet

###### Unterstützte Komponenten

Eingabe, Nummerneingabe, editierbarer Text, Textbereich, Upload, JSON-Editor, JSON-Schema Formular, Switch, Select, Multiselect, Checkbox Gruppe, Cascader, Radio Group, Radio Button, Datum, Datumsbereich, Datumszeit, Datumszeit, Text, Rate, Balkenfortschritt, Circle Fortschritt, Timeline, Divider

###### Beispiel

```JavaScript
input1.clearValue()
```

##### focus()

Wird verwendet, um die Komponente zu fokussieren

###### Unterstützte Komponenten

Eingabe, Nummerneingabe, editierbarer Text, Textbereich, JSON-Editor, Rich-Text-Editor, Radio-Gruppe,

###### Beispiel

```JavaScript
input1.focus()
```

##### validate()

Wenn Sie Validierungsbedingungen für eine Komponente festgelegt haben, z. B. E-Mail-Format, URL-Format, oder Pflichtfeld, kannst du die `validate()` Funktion verwenden, um festzustellen, ob sie die Anforderungen erfüllt. Falls die Anforderungen nicht erfüllt sind, wird eine Validierungsmeldung angezeigt.

Validierungsbedingungen festlegen:

![9](https://cdn.illacloud.com/official-website/img/docs/new-doc/run-script/9.png)

Bestätigungsnachricht anzeigen:

![10](https://cdn.illacloud.com/official-website/img/docs/new-doc/run-script/10.png)

###### Unterstützte Komponenten

Eingabe, Nummerneingabe, editierbarer Text, Textbereich, Hochladen, Auswählen, Multiselekt, Checkbox Gruppe, Radio-Gruppe, Radio-Schaltfläche, Datum, Zeitraum, Formular, Rate

###### Beispiel

```JavaScript
input1.validate()
```

##### clearValidation()

Wird zum Löschen der Validierungsnachricht verwendet.

###### Unterstützte Komponenten

Eingabe, Nummerneingabe, editierbarer Text, Textbereich, Hochladen, Auswählen, Multiselekt, Checkbox Gruppe, Radio-Schaltfläche, Datum, Bereich, Datum, Uhrzeit, Rate

###### Beispiel

```JavaScript
input1.clearValidation()
```

##### setSelectedValue(array)

Wird verwendet, um den ausgewählten Wert von Multiselect festzulegen. Der Parameter sollte ein Array sein.

###### Unterstützte Komponenten

Mehrfachauswahl

###### Beispiel

```JavaScript
multiselect1.setSelectedValue(["Option 1", "Option 3"])
```

##### submit()

Formulardaten abschicken. Wenn die Sendemethode ausgelöst wird, werden zuerst die Formulardaten validiert. Bei Validierungsfehlern wird die Formularabgabe verhindert und die entsprechenden Fehlermeldungen angezeigt. Erst nach erfolgreicher Validierung wird die Einreichung fortgesetzt.

###### Unterstützte Komponenten

JSON-Schema-Formular, Formular

###### Beispiel

```JavaScript
form1.submit()
```

##### umschalten

Wird verwendet, um den Wert von true auf false oder von false auf true zu wechseln.

###### Unterstützte Komponenten

Wechseln

###### Beispiel

```JavaScript
switch1.toggle()
```

##### setStartOfRange(number)

Wird verwendet, um den Standardstartwert des Schiebereglers zu setzen

###### Unterstützte Komponenten

Range Slider

###### Beispiel

```JavaScript
rangeSlider1.setStartOfRange(1)
```

##### setEndOfRange(Zahl)

Wird verwendet, um den Standardwert des Bereichsreglers zu setzen

###### Unterstützte Komponenten

Range Slider

###### Beispiel

```JavaScript
rangeSlider1.setEndOfRange(9)
```

##### setStartWert (string)

Verwendet um den Standardstartwert zu setzen

###### Unterstützte Komponenten

Datumsbereich, Zeitbereich

###### Beispiel

```JavaScript
dateRange1.setStartValue("2023-09-09")
```

##### setEndWert (String)

Verwendet um den Standardwert für den Endwert zu setzen

###### Unterstützte Komponenten

Datumsbereich, Zeitbereich

###### Beispiel

```JavaScript
dateRange1.setEndValue("2023-10-09")
```

##### setValueInArray(Array)

Wird verwendet, um die Schalter zu setzen, deren Wert wahr ist

###### Unterstützte Komponenten

Gruppe wechseln

###### Beispiel

```JavaScript
switchGroup1.setValueInArray(["Option 1", "Option 3"])
```

##### selectPage (Zahl)

Wird verwendet, um den Index der Seite anzuzeigen. Der Index ist eine Zahl, die von 0 beginnt.

###### Unterstützte Komponenten

Tisch

###### Beispiel

```JavaScript
table1.selectPage(1)
```

##### selectZeile (Zahl)

Wird verwendet, um den Index der ausgewählten Zeile zu setzen. Der Index ist eine Zahl, die von 0 beginnt.

###### Unterstützte Komponenten

Tisch

###### Beispiel

```JavaScript
table1.selectZeile (1)
```

##### clearSelection()

Wird verwendet, um die ausgewählte Zeile zu löschen.

###### Unterstützte Komponenten

Tisch

###### Beispiel

```JavaScript
table1.clearSelection()
```

##### setFilters(Array von Objekten, Zeichenkette)

Zum Setzen von Filtern verwendet.

**Der erste Parameter** ist ein Array von Objekten. Jedes Objekt stellt eine Filterbedingung dar. Er enthält drei Attribute:

**`id`** : Dies bezieht sich auf die Id dieser Spalte. Wenn diese Spalte ein Ergebnis einer Datenbankabfrage ist, wäre die ID der Spaltenname, den Sie aus der Abfrage geholt haben. Zum Beispiel, wenn die Abfrage `SELECT email AS user_email FROM users` ist und `email` in `Email` umbenennt, indem das rechte Panel der Tabelle verwendet wird die ID der Spalte wäre immer noch `user_email` wenn diese Spalte manuell mit der Schaltfläche `Spalte hinzufügen` in der rechten Hand hinzugefügt wird, du kannst seine ID im Abschnitt `displayedData` des linken Fensters überprüfen. Normalerweise wird es eine zufällig generierte Zeichenkette sein, die mit `column` beginnt, wie `column-320169e8-225a-482d-9853-ad34b3040220`.

**`filterFn`** : der Match-Typ des Filters. Wir unterstützen die folgenden Typen: `equalTo`, was die gleiche ist wie `=` in SQL, `notEqualTo` ist identisch mit `<>` in SQL, `contains` und entspricht dem `LIKE '%value%'` in SQL, `does NotContain` was das gleiche ist wie `NOT LIKE '%value%'` in SQL, `lessThan` ist identisch mit `<` in SQL, `notLessThan`, was das gleiche ist wie `>=` in SQL, `notMoreThan` ist identisch mit `<=` in SQL, `empty` was das gleiche ist wie `IS NULL` in SQL, `notEmpty` ist das gleiche wie `IST NICHT NULL` in SQL, `before` wird verwendet, um Zeit oder Datum zu vergleichen, was das gleiche ist wie `>` in SQL, `after` wird verwendet, um Zeit oder Datum zu vergleichen, was dasselbe ist wie `<` in SQL.

**`value`** : wird verwendet, um den zu vergleichenden Wert zu setzen.

**Der zweite Parameter:** Wenn es mehrere Filterbedingungsobjekte gibt, bestimmt er, ob sie eine "AND" Beziehung oder eine "ODER" Beziehung haben. Setze es als String `and` für eine "AND"-Beziehung, und `oder` für eine "OR"-Beziehung.

###### Unterstützte Komponenten

Tisch

###### Beispiel

```JavaScript
table1.setFilters([{"id":"month", "filterFn":"equalTo", "value":"May"}, {"id":"month", "filterFn":"equalTo", "value":"Juni"}], "oder")
```

##### clearFilters()

Zum Löschen aller Filter

###### Unterstützte Komponenten

Tisch

###### Beispiel

```JavaScript
table1.clearFilters()
```

##### setSort(string, string)

Verwendet um die Sortierung festzulegen. **Der erste Parameter** ist die ID der zu sortierenden Spalte, die ein String-Wert ist. Die Id ist die gleiche wie die Id, die in setFilters(array of objects, string) erwähnt wird. **Der zweite Parameter** ist die Sortierreihenfolge aufsteigend oder absteigend. Es wird mit einem String Wert `ascend` oder `descend` gesetzt.

###### Unterstützte Komponenten

Tisch

###### Beispiel

```JavaScript
table1.setSort("Einnahmen", "absteigend")
```

##### setCurrentViewKey(string)

Verwenden Sie den Schlüssel, der ein String-Wert der Ansicht ist, um die angezeigte Ansicht zu setzen.

###### Unterstützte Komponenten

Container

###### Beispiel

```JavaScript
container1.setCurrentViewKey("Ansicht 2")
```

##### setCurrentViewIndex (Zahl)

Verwenden Sie den Index, der ein Zahlenwert ab 0 der Ansicht ist, um die angezeigte Ansicht festzulegen.

###### Unterstützte Komponenten

Container

###### Beispiel

```JavaScript
container1.setCurrentViewIndex(1)
```

##### zeige NextView(boolean)

Wird verwendet, um die nächste Ansicht anzuzeigen. Der Parameter ist ein boolescher Wert, der bestimmt, ob die Schleife vom Ende zurück zum Anfang zurückgelegt werden soll.

###### Unterstützte Komponenten

Container

###### Beispiel

```JavaScript
container1.showNextView(wahr)
```

##### zeige NextVisibleView(boolean)

Wird verwendet, um die nächste sichtbare Ansicht anzuzeigen. Im Gegensatz zum Verhalten der `showNextView(boolean)` Funktion bietet diese Funktion eine verbesserte View-Switching-Funktion, indem sie alle versteckten Ansichten während des Übergangs überspringt. Der Parameter ist ein boolescher Wert, der bestimmt, ob die Schleife vom Ende zurück zum Anfang zurückgelegt werden soll.

###### Unterstützte Komponenten

Container

###### Beispiel

```JavaScript
container1.showNextVisibleView(false)
```

##### showPreviousView()

Wird verwendet, um die vorherige Ansicht anzuzeigen. Der Parameter ist ein boolescher Wert, der bestimmt, ob die Schleife vom Start bis zum Ende zurückgelegt werden soll.

###### Unterstützte Komponenten

Container

###### Beispiel

```JavaScript
container1.showPreviousView(true)
```

##### showPreviousVisibleView()

Wird verwendet, um die vorherige sichtbare Ansicht anzuzeigen. Im Gegensatz zum Verhalten der `showPreviousView(boolean)` Funktion bietet diese Funktion eine verbesserte View-Switching-Funktion, indem sie alle versteckten Ansichten während des Übergangs überspringt. Der Parameter ist ein boolescher Wert, der bestimmt, ob die Schleife vom Start bis zum Ende zurückgelegt werden soll.

###### Unterstützte Komponenten

Container

###### Beispiel

```JavaScript
container1.showPreviousVisibleView(true)
```

##### reset()

Wird verwendet, um den Wert auf den Standardwert zurückzusetzen.

###### Unterstützte Komponenten

Formular

###### Beispiel

```JavaScript
form1.reset()
```

##### resetValue()

Wird verwendet, um den Wert auf den Standardwert zurückzusetzen.

###### Unterstützte Komponenten

Schritte

###### Beispiel

```JavaScript
steps1.resetValue()
```

##### openModal()

Wird zum Öffnen eines Modals verwendet

###### Unterstützte Komponenten

Modal

###### Beispiel

```JavaScript
modal1.openModal()
```

##### closeModal()

Wird zum Schließen eines Modals verwendet

###### Unterstützte Komponenten

Modal

###### Beispiel

```JavaScript
modal1.closeModal()
```

##### setImageUrl(string)

Wird verwendet, um die URL für die Bildquelle festzulegen. Es kann ein Base64 oder eine URL sein, die auf ein Bild im Web zeigt.

###### Unterstützte Komponenten

Bild

###### Beispiel

```JavaScript
image1.setImageUrl("https://cloud-api.illacloud.com/drive/f/83095b77-1180-487e-86db-ff117e6083e5")
```

```JavaScript
image1.setImageUrl("data:image/png;base64,iVBORw0KGgoAA...II=")
```

##### clearReplyMessage()

Wird verwendet, um die zitierte Antwort über dem Eingabefeld zu löschen.

###### Unterstützte Komponenten

Chat

###### Beispiel

```JavaScript
chat1.clearReplyMessage()
```

##### setPrimaryValue(Zahl)

Wird verwendet, um den Primärwert der Statistiken festzulegen.

###### Unterstützte Komponenten

Statistiken

###### Beispiel

```JavaScript
statistic1.setPrimaryValue(100)
```

##### resetPrimaryValue()

Wird verwendet, um den Primärwert auf den Standardwert der Statistiken zurückzusetzen.

###### Unterstützte Komponenten

Statistiken

###### Beispiel

```JavaScript
statistic1.resetPrimaryValue()
```

##### setFileUrl(string)

Verwendet um die URL der Datei zu setzen. Es kann ein Base64 oder eine URL sein, die auf ein PDF im Web verweist.

###### Unterstützte Komponenten

PDF

###### Beispiel

```JavaScript
pdf1.setFileUrl("https://upload.wikimedia.org/wikipedia/commons/e/ee/Guideline_No._GD-Ed-2214_Marman_Clamp_Systems_Design_Guidelines.pdf")
```

##### play()

Wird verwendet, um Video oder Audio wiederzugeben.

###### Unterstützte Komponenten

Video, Audio

###### Beispiel

```JavaScript
video1.play()
```

##### pause()

Verwendet um Video oder Audio zu pausieren.

###### Unterstützte Komponenten

Video, Audio

###### Beispiel

```JavaScript
video1.pause()
```

##### showControls(boolean)

Wird verwendet, um die Steuerelemente anzuzeigen.

###### Unterstützte Komponenten

Video

###### Beispiel

```JavaScript
video1.showControls(true)
```

##### setVideoUrl(string)

Verwendet um die URL des Videos zu setzen. Es kann ein Base64 oder eine Video-URL auf Medienwebseiten sein.

###### Unterstützte Komponenten

Video

###### Beispiel

```JavaScript
video1.setVideoUrl("https://youtu.be/8sUovZlBh_M")
```

##### setVolume(Zahl)

Verwendet um die Lautstärke zu setzen. Der Parameter ist ein Zahlenwert zwischen 0 und 1.

###### Unterstützte Komponenten

Video, Audio

###### Beispiel

`video1.setVolume(0.5)`

##### setSpeed(Zahl)

Wird verwendet, um die Geschwindigkeit festzulegen. Der Parameter ist ein Zahlenwert zwischen 0.25 und 2.

###### Unterstützte Komponenten

Video, Audio

###### Beispiel

```JavaScript
video1.setSpeed(0.5)
```

##### setLoop(boolean)

Wird verwendet, um die Wiedergabe zu schleifen.

###### Unterstützte Komponenten

Video, Audio

###### Beispiel

```JavaScript
video1.setLoop(falsch)
```

##### seekTo(Zahl)

Wird verwendet, um eine bestimmte Zeitposition zu suchen. Der Parameter ist ein Zahlenwert, der in Sekunden (s) gemessen wird. Zum Beispiel, um die Zeitposition 5:35 zu suchen, würden Sie den Wert 335 verwenden.

###### Unterstützte Komponenten

Video, Audio

###### Beispiel

```JavaScript
video1.seekTo(335)
```

##### mute(boolean)

Wird benutzt um stumm zu setzen oder nicht.

###### Unterstützte Komponenten

Video, Audio

###### Beispiel

```JavaScript
video1.mute(true)
```

##### setAudioUrl(string)

Verwendet um die Audio-URL zu setzen. Es kann ein Base64 oder eine Video-URL auf Medienwebseiten sein.

###### Unterstützte Komponenten

Audio

###### Beispiel

```JavaScript
video1.setAudioUrl("https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3")
```

##### slickNext()

Wird verwendet, um zum nächsten Bild zu wechseln.

###### Unterstützte Komponenten

Carousel

###### Beispiel

```JavaScript
carousel1.slickNext()
```

##### slickPrevious()

Wird verwendet, um zum vorherigen Bild zu wechseln.

###### Unterstützte Komponenten

Carousel

###### Beispiel

```JavaScript
carousel1.slickPrevious()
```

##### setSrc()

Wird verwendet, um die Quell-URL von IFrame zu setzen.

###### Unterstützte Komponenten

IFrame

###### Beispiel

```JavaScript
iframe1.setSrc("https://www.nasa.gov/")
```

##### setMarkers(array)

Wird verwendet, um die Markierungen auf der Karte zu setzen. Der Parameter sollte ein Array von Objekten sein. Die Felder Breitengrad und Längengrad in jedem Objekt sollten identisch sein mit den Feldern, die im rechten Panel gesetzt werden

###### Unterstützte Komponenten

Karte

###### Beispiel

```JavaScript
map1.setMarkers([
{
    "lat":57.14009563040699,
    "lng":-114.6018657894011
 },{
     "lat":55.40432513713809,
     "lng":-112.98252263795787
 },{
     "lat":57.84128888545,
     "lng":-103.3518657894011
 }
])
```

##### resetMarkers()

Wird verwendet, um die Markierungen auf die Standardmarkierungen zurückzusetzen.

###### Unterstützte Komponenten

Karte

###### Beispiel

```JavaScript
map1.resetMarkers()
```

### Gehe zu URL

#### Struktur

```JavaScript
utils.goToURL({
url:string,
newTab:boolean
})
```

#### Beschreibung

Verwende ein Objekt um es zu setzen. Es gibt 2 Attribute im Objekt. Das erste Attribut ist `url`. Sein Wert ist eine komplette URL, einschließlich 'https://' oder 'http://', und sie ist von Typ String. Das zweite Attribut ist `newTab`. Es ist ein boolescher Einstellung, ob die URL in einem neuen Tab geöffnet werden soll.

#### Beispiel

```JavaScript
utils.goToURL({
url:"https://www.illacloud.com",
newTab:true
})
```

### In Zwischenablage kopieren

#### Struktur

```JavaScript
utils.copyToClipboard(string)
```

#### Beschreibung

Zum Kopieren einer Zeichenkette verwendet

#### Beispiel

```JavaScript
utils.copyToClipboard ("Der zu kopierende Text")
```

### Router festlegen

#### Struktur

```JavaScript
utils.setRouter({
pagePath:string;
viewPath:string
})
```

#### Beschreibung

Wird verwendet, um die Seite in ILLA Apps zu ändern. Es gibt 2 Attribute im Objekt.

Das erste Attribut ist `pagePath`. Der Wert von pagePath wird auf dem folgenden Screenshot ohne `/` angezeigt.

![11](https://cdn.illacloud.com/official-website/img/docs/new-doc/run-script/11.png)

Das zweite Attribut ist `viewPath`. Der Wert des viewPath wird auf dem folgenden Screenshot ohne `/` angezeigt.

![12](https://cdn.illacloud.com/official-website/img/docs/new-doc/run-script/12.png)

#### Beispiel

```JavaScript
utils.setRouter({
pagePath:"page2", 
viewPath:"subpage1"
})
```

### In ILLA Laufwerk speichern

#### Struktur

```JavaScript
utils.saveToILLADrive({
fileName:string,
fileData:string,
fileType:string,
folder:string,
allowAnonymous:boolean,
replace:boolean
})
```

#### Beschreibung

Wird verwendet, um eine Datei im ILLA Laufwerk zu speichern.

`fileName`: Name mit Erweiterung der Datei nach dem Hochladen in ILLA Drive.

`fileData`: Base64 oder Rohdateien, wie JSON, CSV, Klartext, etc.

`fileType`: Einer der folgenden Werte: Auto, Plain text, JPEG, PNG, SVG, JSON, CSV, TSV, Excel(.xlsx). Bei Verwendung von "Auto" werden wir versuchen, den Dateityp anhand der Dateierweiterung oder der Datei-Daten zu ermitteln.

`Ordner`: Zielordner für Datei-Upload. Leer lassen um in den Root-Ordner hochzuladen.

'allowAnonymous': Wird benutzt um festzulegen, ob anonymer Benutzerzugriff erlaubt ist. Wenn Sie Ihre Anwendung als öffentlich einstellen wollen, müssen anonyme Zugriffe aktiviert sein. Zur Sicherheit Ihrer Daten müssen Dateien oder Ordner anonym im anonymen Ordner abgelegt werden.

![13](https://cdn.illacloud.com/official-website/img/docs/new-doc/run-script/13.png)

`replace`: Wird zum Setzen verwendet, wenn es Dateien mit dem gleichen Namen im selben Verzeichnis gibt, ersetzen Sie die vorhandene Datei durch eine neue Datei oder benennen Sie die neue Datei automatisch um.

#### Beispiel

```JavaScript
//upload1 verwenden, um Dateien vom Gerät auszuwählen und in den App-Ordner hochzuladen. 
utils.saveToILLADrive({
fileName:upload1.files[0].name,
fileData:upload1. iles[0].dataURI,
fileType:"PNG",
Ordner:"apps",
allowAnonymous:false,
ersetzen: true
})
```

```JavaScript
//Laden Sie eine Textdatei hoch.  
utils.saveToILLADrive({
fileName:"test.txt",
fileData:"abc",
fileType:"Plain text",
Ordner:"apps",
allowAnonym:false,
replace:true
})
```

### Von ILLA Drive herunterladen

#### Struktur

```JavaScript
utils.downloadFromILLADrive({
    DownloadInfo:[{
        tinyURL:string,
        fileID:string
        }],
    asZip:boolean
})
```

#### Beschreibung

Zum Herunterladen von Dateien von ILLA Drive.

`downloadInfo`: ein Array von Objekten, einschließlich tinyURL und fileID. Du kannst die Komponente Drive Datei auswählen um Dateien aus dem ILLA Drive auszuwählen und die `downloadInfo` via drivePicker1.value zu erhalten

`asZip`: verwendet, um festzulegen, ob die Dateien als Zip herunterzuladen.

#### Beispiel

```JavaScript
utils.downloadFromILLADrive({
    downloadInfo:drivePicker1.value,
    asZip:true
})
```

### Download

#### Struktur

```JavaScript
utils.downloadFile({
    fileType:string,
    fileName:string,
    data:anyType
})
```

#### Beschreibung

Wird verwendet, um Daten auf Ihr Gerät zu exportieren.

`fileType`: Einer der folgenden Werte: Auto, Plain text, JPEG, PNG, SVG, JSON, CSV, TSV, Excel(.xlsx). Bei Verwendung von "Auto" werden wir versuchen, den Dateityp anhand der Dateierweiterung oder der Datei-Daten zu ermitteln.

`fileName`: ein Name mit Erweiterung.

`data`: Daten der Datei.

#### Beispiel

```JavaScript
//Laden Sie die Daten der Tabelle als CSV
utils.downloadFile({
    fileType:"CSV",
    fileName:"text.csv",
    data: table2.dataSourceJS
})
```

```JavaScript
//Bild
utils.downloadFile({
    fileType:"PNG",
    fileName:"text.png",
    data: image1.imageSrc
})
```

### Globale Daten festlegen

#### Globalen Datenwert festlegen

##### Struktur

```JavaScript
utils.setGlobalDataValue({
key:string, 
value:any
})
```

##### Beschreibung

Verwendet um den Wert von gobalData zu setzen

`key`: der displayName von globalData wie der folgende Screenshot zeigt.

![14](https://cdn.illacloud.com/official-website/img/docs/new-doc/run-script/14.png)

`value`: der zu setzende Wert

##### Beispiel

```JavaScript
//verwende input5, um den Benutzereingabewert zu erhalten und als Wert von stringState zu setzen. 
utils.setGlobalDataValue({
key:"stringState", 
value:input5.value
})
```

#### Globale Daten setzen in

##### Struktur

```JavaScript
utils.setGlobalDataIn({
key:string, 
path:string, 
value:any
})
```

##### Beschreibung

Wenn die globalen Daten ein Array oder Objekt sind, setGlobalDataIn verwenden, um den Wert zu ändern, der einem bestimmten Index in einem Array entspricht, oder um den Wert zu ändern, der einem bestimmten Schlüssel in einem Objekt entspricht.

`key`: der displayName von globalData.

`path`: der Index des Wertes, den Sie im Array oder dem Schlüssel des Objekts aktualisieren wollen. Zum Beispiel, um den zweiten Wert im Array zu setzen, ist der Pfad 1 (der Index). Um den Wert des Attributs Key1 zu setzen, ist der Pfad key1.

`value`: der zu setzende Wert

##### Beispiel

```JavaScript
//objectState ist ein Objekt: {"key1":"value1", "key2":"value2"}, um die 
utils.setGlobalDataIn({
key:"objectState", 
path:"key2", 
value:input7.value
})
```

## Integrierte Bibliotheken

| **Bibliothek**     | **Doc**                                                               | **Wie man es im ILLA Builder verwendet** | **Beispiel**                                                |
| ------------------ | --------------------------------------------------------------------- | ---------------------------------------- | ----------------------------------------------------------- |
| Lodash             | https://lodash.com/docs/4.17.15                                       | `_`                                      | `_.concat(array, 2, [3], [[4]]);`                           |
| Tag.js             | https://day.js.org/                                                   | 'dayjs'                                  | `dayjs(new Date())`                                         |
| uuuid              | <https://www.npmjs.com/package/uuid>                                  | "uuid"                                   | `uuid.parse()`                                              |
| numbro             | https://numbrojs.com/                                                 | "numbro"                                 | `numbro(1000).format({thousandSeparated: true})`            |
| Papa               | https://www.papaparse.com/docs                                        | `Papa`                                   | `Papa.parse(url, {download: true // rest of config ... })`  |

## Wert der Aktionen, Komponenten...

Benutze `displayName.xxx` um den Wert von Aktionen, Komponenten oder anderen globalen Daten zu verwenden. Bitte beachten Sie, dass diese Variablen im Ausführungsskript ohne `{{` Referenzen direkt aufgerufen werden können.

```JavaScript
utils.downloadFromILLADrive({
    downloadInfo:{{drivePicker1.value}},//❌ 
    asZip:true
})

utils.downloadFromILLADrive({
    downloadInfo:drivePicker1.value,//:check_mark_button:
    asZip:true
})
```
