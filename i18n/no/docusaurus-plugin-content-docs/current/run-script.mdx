---
title: Kjør skript
description: Ved å utløse "Kjør Script" gjennom en hendelsesbehandler, kan du oppnå sekvensiell utførelse av operasjoner og utføre forskjellige handlinger basert på forskjellige betingelser.
---

# ow-to

<div style={{
    position: "relative",
    paddingBottom: "calc(56.81216931216932% + 41px)", 
    height: 0
    }}><iframe src="https://demo.arcade.software/r6dDc3eLjVfcrx2yGZnQ?embed" frameborder="0" loading="lazy" webkitallowfullscreen mozallowfullscreen allowfullscreen style={{position: "absolute", top: 0, left: 0, width: 100%, height: 100%, colorScheme: "light"}}></iframe></div>

## Demonstrasjon

<div style={{
    position: "relative",
    paddingBottom: "calc(56.81216931216932% + 41px)", 
    height: 0
    }}><iframe src="https://builder.illacloud.com/illacloud_demo/deploy/app/ILAfx4p1C7Ac" frameborder="0" loading="lazy" webkitallowfullscreen mozallowfullscreen allowfullscreen style={{position: "absolute", top: 0, left: 0, width: 100%, height: 100%, colorScheme: "light"}}></iframe></div>

### Kjør og gaffel

https://illa.ai/app/ILAfx4p1C7Ac/detail

## Bruk saken: Logg inn og omdirigere til forskjellige sider

<div style={{
    position: "relative",
    paddingBottom: "calc(56.81216931216932% + 41px)", 
    height: 0
    }}><iframe src="https://builder.illacloud.com/illacloud_demo/deploy/app/ILAex4p1C7Eh" frameborder="0" loading="lazy" webkitallowfullscreen mozallowfullscreen allowfullscreen style={{position: "absolute", top: 0, left: 0, width: 100%, height: 100%, colorScheme: "light"}}></iframe></div>

### Kjør og gaffel

https://illa.ai/app/ILAex4p1C7Eh/detail

# ILLA custom functions

## Utløser handling

### Struktur

```JavaScript
konst eksempeldata = venter på actionName.trigger();
```

Handlingsnavnet er strengen som vises i skjermdumpen, som følger.

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=NTQzYlllNmQ2NTdNTM1ZGZjZDU3MTNjMjk3ZTI1ZfT2RwQTNkaEZSNHdpSmFOeVlmcVJPN2NaOXZ6N1NnBfV9rZrZW46TGhZWhZWJEJEYWb00JOVYOVOV2YOW_TZOWM_MXTNNN_MG2MXOW_MXTISTISTISTISTISTISTISTISTISTISTISTISTIS)

### Beskrivelse

I Kjør skript er variabelen `exampleData` det samme som `actionName.data`.

### Eksempel

For eksempel bruker vi handlingen `postgresql1` til å spørre James' e-post.

!\[](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=NzdlOTEyZjBmM2M3MWJlZTMwYTIwZjkzMTQ1OTBhM2VfZEVEVNeNeTJRZGE2TFRObkNXTlV0OHdFOE85YzFYaU1JaVfVG9rZW46VU0yWI4RHHbWbzQ2QV2QV4VRVcVcVNGNNZGN_GN_GN_GN_GN_MXOwMGN_MXSMSN_G2MXSMSSIKCHCHOWMXS_DCHCHCHCHCHFFFF

Utløs handlingen via Kjør Script

!\[](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=YTQzOTE3NDI3YzU1MzVhOTY2ZTg4ODBjOWUyZmMxYzlfdW9uYzR5VXhLYVMXRlYnpU2xTTHo3WHJEZUpnaEJfVG9rZW46V1V1VhGJVVcGdyb2ZFZGZF4OGRHHHH3KKKNNNNNNNNNN_BXN_21G2TXTXTXV3KMOTKMO_G21OWMXS_DCH_DCH_DCH_MXTISTISTT

`postgresql1.data` er

```JSON
[
  {
    "email": "James@outlook.com",
    "id": "1",
    "img": "https://images.pexels.com/photos/1222271/pexels-photo-1222271.jpeg?auto=compress&cs=tinysrgb&w=1600",
    "name": "James"
  }
]
```

Når vi må bruke JavaScript for å hente en e-post i komponentens Inspeksjon eller andre steder, kan vi bruke følgende: `{{postgresql1.data[0].email}}`

!\[](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=ZDQyMWExZThhMzU5MGQwZTVlMDI0ZmUwNzk3NGYzZGJfdVdxNVFCS2hSMDY4YHWENpeE1LVFlFmp0cHVtRzlfV9rZW46RUpjTGJyZyNA4b13dQ0MMMN4V3TTTNTTTTTTTTTTTTTTB2TTTTTTB2TTTB2TISTISTISTISTISTISTISTISTIST1

And when we need to use the email in the "Run Script" after executing the statement `const exampleData = await postgresql1.trigger();`, we can directly use `exampleData[0].email`.

Show a success notification when the query is successful and the result is unique. Show a failure notification when the query result is not unique or when it fails.

!\[](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=YjEyZDk4MjdjZjE5ZTdlZGQ0NzQ4NmI5ZDQ5MDE0ODBfTWhTdkI1VII3VI1VI1VI1VlNqYmtvNzvzmoVRjRYbVZfVZfV9rZW46VmwmNkTTNLd3BDRNNNH3SNH3TUM_G_G_G_G_G_M_G_G_G_G_M_M_GN_G_G_G_M_G_GGG_G_G_G_TN_G_M

```JSON
const exampleData = await postgresql1.trigger();
///Vis e-post via varsling når lengden på den resulterende listen er 1.
if(eksempeldata.length==1){
utnytter. howNotification(
  {
    type:"info",
    tittel:"Hentet vellykket",
    beskrivelse:exampleData[0]. mail,
    varighet: 4500}
);
  }else{
  //else, vis en mislykket varsling
verktøy. howtification(
  {
    type:"info",
    title:"Feil ved hente",
    varighet: 4500
  }

};
```

## Vis varsel

### Struktur

```JavaScript
utils.showNotification(
{
type: "info",
-tittel: "gratulererer",
beskrivelse: "Hentet vellykket",
varighet: 4500
}
)
```

### Beskrivelse

| Parametre    | Beskrivelse                                             | Type data                                                                        | Eksempel                               |
| ------------ | ------------------------------------------------------- | -------------------------------------------------------------------------------- | -------------------------------------- |
| type         | Brukes til å angi ikontypen i meldingen.                | Streng, en av følgende verdier:"info"`default`"success""error""warning"          | type: "info"                           |
| tittel       | Brukes til å angi tittelen på varselet                  | Streng                                                                           | title: "Gratulerer"                    |
| beskrivelse  | Brukes til å angi beskrivelse                           | Streng                                                                           | beskrivelse: "Hentet vellykket"        |
| varighet     | Brukes til å angi visningens varighet av meldingen.     | Antall, i millisekunder (ms)                                                     | varighet: 4500                         |

### Eksempel

Vis varsling når du klikker på knappen:

!\[](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=NmNlZWMxNzFiMWVjZjjjZDI0MjgzMzAwNGE2ODQzNjdfRFRFbXhKaTk2a2a2RIVFh2dnI0ZW5UcHVtZUEwZRHa0ZfV9rZW46THZYkWJnKFSMHONH4QTNNNNNNrrrrrrrrrTKMXTCWODTZOWCOTCW2MXTISTISTISLTISTISLTISTISTISTISLTISTISTISTISTISTISTISL

Det er samme som Vis varselhandlinger, som følgende skjermbilde viser:

!\[](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=NTY2ZmNmMDQ2MjE5ZjM2NTTTTZlMjZmYjk5YjA4ZTBfdExSYUZqTVVzMUtiZnBjaEVqZFRYSGdIcklLSmtMSXFfV9rZW46Q21SdJpb0bpV41Vl1OGVnVnGZdZZrdZOw2MXVZMvzzzzzzzzfor MXWMXXXWMXWMXWMXXXXXXWMXXXXWMXS_DMXXWMXWMXX

## Styre komponent

### Struktur

```JavaScript
componentDisplayName.function()
```

komponent DisplayName er strengen som vises i skjermdumpen, som følger.

!\[](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=ODUwYzM1ZDVlMFjYjhMThjZGZjNExYmIyOWI4NjVfdGtlbTRXS3ZNall1aTRzWWJoZGtwVTl2VlRBa3VYYW5fV9rZW46V3o5VWJUaV4b3IzUlp4VWFHMHHHHHHNTTTISE3TISTISTIS-3TISTIS, MPDTISTIS-4TISTISTISTISTISTISTISTIS-1 MPDMPDMPDMPDMXTIS-ITI4TISTIS-

### Beskrivelse

#### setValue(parameter)

Used to set the value. The parameter could be a `string` or `number` or `object` or `boolean` or `array` which depends on the component type.

##### Støttede komponenter

| Komponent                                                                                                                                                                  | Type parameter                                                     | Eksempel                                                                                                                                                                                     |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Innskriving, Redigerbart tekst, tekstområde, Divider, Velge, Radiogruppe, Radioknapp for Radio Buttonm, Dato, Dato og tid, Skritt, Tekst, Linje Progress, Sirkel Framdrift | `string`                                                           | `input1.setValue("Hallo world")`                                                                                                                                                             |
| JSON Editor                                                                                                                                                                | `string`                                                           | `jsonEditor1.setValue(`{ "firstName": "Chuck", "lastName": "Norris", "age": 80, "bio": "Roundhouse kicking asses since 1940", "password": "noneed", "telephone": "1-800-KICKASS" }`)`        |
| Tidslinje                                                                                                                                                                  | `streng`, Separer verdiene til forskjellige noder via komma (,)    | `timeline1.setValue("Den første milepælen,Den andre milepælen")`                                                                                                                             |
| Tall inngang, skyveknapp, rate                                                                                                                                             | `nummer`                                                           | `numberInput1.setValue(1000)`                                                                                                                                                                |
| Bytt                                                                                                                                                                       | `boolean`                                                          | `switch1.setValue(false)`                                                                                                                                                                    |
| JSON skjema, skjema                                                                                                                                                        | `objekt`                                                           | `jsonSchemaForm1.setValue({ "firstName": "Chuck", "lastName": "Norris", "age": 80, "bio": "Roundhouse kicking asses since 1940", "password": "noneed", "telephone": "1-800-KICKASS" })`      |
| Avkrysningsboks gruppe, plassholder                                                                                                                                        | `array`                                                            | `cascader1.setValue(["media_source_2";"campaign_2-1"])`                                                                                                                                      |

#### clearValue()

Brukes til å fjerne verdien

##### Støttede komponenter

Innvift, Antall innput, Redigerbar tekst, tekstområde, Opplasting, JSON Editor, JSON Schema Form, Switch, Velg, Multiselect, Checkbox Group, Skalad, Radiogruppe, Radioknapp, Dato, Datonivå, Datotid, Datotid, Tekst, Rotasjon, Bar Progresjon, Sirkel Fremdrift, Timeline, Divider

##### Eksempel

```JavaScript
input1.clearValue()
```

#### fokus()

Brukes til å fokusere på komponenten

##### Støttede komponenter

Innskriving, tallinnput, redigerbart tekstområde, JSON-redigeringsprogram med tekst, Radio Group,

##### Eksempel

```JavaScript
input1.focus()
```

#### validate()

When you have set validation conditions for a component, such as email format, URL format, or required field, you can use the `validate()` function to determine if it meets the requirements. If it does not meet the requirements, a validation message will be displayed.

Angi valideringsbetingelsene:

!\[](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=YjIwNTcyN2VjZDZkOTYzNGFlMzM0ZmVhZjJiYmI2MThfMUQ0ME5hZ0dOMEVVEcHVxdm03cTdsVHBOcvaktTZfV9rZW46QzhzhzhzhMzemeXht1ESnKzdMXV2TISTISTIS/MXOMXOMXV2MXOMXXV2MXV2MXV2MXV2MXXXXXXWMXWMXXWMXS\_

Vis valideringsmelding:

!\[](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=OWQ1NzRmMTkxOGE3NjFjMTYzYTYxNGMzZTE0N2QyY2ZfNEt4Q3ozbU1HRDJOYTVHNmYmpUZWRDMTZzdmmR0N3lfV9rrfV9rZW46SDhFOWqdWUb2tbH4V3V3V3NnMUMGMNNMG_G_GMG_G2MXOw2MGG_MG_MG_GG_G_G_G_MG_D_D_D_D_G_D_MG_G_G

##### Støttede komponenter

Innhold, Nummerinngang, Redigerbar tekst, Tekstområde, Opplasting, Velg, Multiselect, Checkbox-gruppe, Radioknapp Dato, Dato Range, Dato og Dato, Dato & Tidspunkt, Rang

##### Eksempel

```JavaScript
input1.validate()
```

#### clearation()

Brukes til å fjerne valideringsmeldingen.

##### Støttede komponenter

Innhold, Nummerinngang, Redigerbart tekst, tekstområde, Opplasting, Velg, Flervalg, Avmerkingsavmerkingsgruppe, Radioknapp, Dato, Dato Område, Dato og Tidsavgang, Rangering

##### Eksempel

```JavaScript
input1.clearValidation()
```

#### setectedValue(array)

Used to set the selected value of Multiselect. The parameter should be an array.

##### Støttede komponenter

Flervalg

##### Eksempel

```JavaScript
multiselect1.setSelectedValue(["Alternativ 1", "Alternativ 3"])
```

#### submit()

Submit form data. When the submit method is triggered, we will first validate the form data. If there are validation errors, the form submission will be prevented, and the corresponding error messages will be displayed. Only after successful validation does the submission proceed.

##### Støttede komponenter

JSON skjema, skjema

##### Eksempel

```JavaScript
form1.submit()
```

#### toggle()

Brukt til å skru verdien fra sann til usann eller fra falsk til sann.

##### Støttede komponenter

Bytt

##### Eksempel

```JavaScript
switch1.toggle()
```

#### setStartOfRange(number)

Brukes til å angi standard startverdien for glidebryteren

##### Støttede komponenter

Range Slider

##### Eksempel

```JavaScript
rangeSlider1.setStartOfRange(1)
```

#### EndOfRange(nummer)

Brukes til å angi standard sluttverdi for glidebryteren

##### Støttede komponenter

Range Slider

##### Eksempel

```JavaScript
rangeSlider1.setEndOfRange(9)
```

#### setStartValue(streng)

Brukt for å angi standard startverdi

##### Støttede komponenter

Dato rekkevidde, Tidsintervall

##### Eksempel

```JavaScript
dateRange1.setStartValue("2023-09-09")
```

#### setEndValue(streng)

Brukt til å angi standard sluttverdi

##### Støttede komponenter

Dato rekkevidde, Tidsintervall

##### Eksempel

```JavaScript
dateRange1.setEndValue("2023-10-09")
```

#### setValueInArray(array)

Brukes til å angi bryterne med sann verdi

##### Støttede komponenter

Bytt gruppe

##### Eksempel

```JavaScript
switchGroup1.setValueInArray(["Alternativ 1", "Alternativ 3"])
```

#### selectPage(nummer)

Used to set the index of the page to display. The index is a number starting from ０.

##### Støttede komponenter

Tabell

##### Eksempel

```JavaScript
tabell1.selektPage(1)
```

#### selectRow(nummer)

Used to set the index of selected row. The index is a number starting from ０.

##### Støttede komponenter

Tabell

##### Eksempel

```JavaScript
tabell1.selectRow(1)
```

#### clearSelection()

Brukes til å tømme den valgte raden.

##### Støttede komponenter

Tabell

##### Eksempel

```JavaScript
table1.clearection()
```

#### setFilter(liste med objekter, streng)

Brukes til å angi filtre.

**The first parameter** is an array of objects. Each object represents a filtering condition. It includes three attributes:

**`id`** : This refers to the id of this column. If this column is a result of a database query, then the id would be the column name you retrieved from the query. For example, if the query is `SELECT email AS user_email FROM users` and rename `email` as `Email` using the right-hand panel of the table, the id of the column would still be `user_email` If this column is manually added using the `Add Column` button in the right-hand panel, you can check its id in the `displayedData` section of the left-hand panel. Typically, it will be a randomly generated string starting with `column` such as `column-320169e8-225a-482d-9853-ad34b3040220`.

**`filterFn`** : the match type of the filter. We support the following types: `equalTo` which is the same as `=` in SQL, `notEqualTo` which is the same as `<>` in SQL, `contains` which is the same as `LIKE '%value%'` in SQL, `does NotContain` which is the same as `NOT LIKE '%value%'` in SQL, `lessThan` which is the same as `<` in SQL, `notLessThan` which is the same as `>=` in SQL, `notMoreThan` which is the same as `<=` in SQL, `empty` which is the same as `IS NULL` in SQL, `notEmpty` which is the same as `IS NOT NULL` in SQL, `before` is used to compare time or date, which is the same as `>` in SQL, `after` is used to compare time or date, which is the same as `<` in SQL.

**\<3>** \*\*`verdi`: brukt for å sette verdien til sammenligning.

**The second parameter:** when there are multiple filter condition objects, it determines whether they have an "AND" relationship or an "OR" relationship. Set it as a string `and` for an "AND" relationship, and `or` for an "OR" relationship.

##### Støttede komponenter

Tabell

##### Eksempel

```JavaScript
table1.setFilters([{"id":"month", "filterFn":"equalTo", "value":"May"}, {"id":"month", "filterFn":"equalTo", "value":"June"}], "eller")
```

#### clearFilters()

Brukes til å fjerne alle filtre

##### Støttede komponenter

Tabell

##### Eksempel

```JavaScript
tabell1.clearters()
```

#### setSort(string, string)

Used to set the sort. **The first parameter** is the id of the column to sort which is a string value. The id is the same as the id mentioned in setFilters(array of objects, string). **The second parameter** is the sorting order, either ascending or descending. It is set using a string value `ascend` or `descend`.

##### Støttede komponenter

Tabell

##### Eksempel

```JavaScript
tabell1.setSort("inntekter", "bedrift")
```

#### setCurrentViewKey(string)

Bruk nøkkelen som er en strengverdi av visningen for å angi visningen.

##### Støttede komponenter

Beholder

##### Eksempel

```JavaScript
container1.setCurrentViewKey("View 2")
```

#### setCurrentViewIndex(number)

Bruk indeksen som er en tallverdi fra 0 av visningen for å angi visningen.

##### Støttede komponenter

Beholder

##### Eksempel

```JavaScript
container1.setCurrentViewIndex(1)
```

#### UtvisningVisning (boolsk)

Used to show the next view. The parameter is a boolean value that determines whether to loop from the end back to the start.

##### Støttede komponenter

Beholder

##### Eksempel

```JavaScript
container1.showNextView(sann)
```

#### Utsikt (boolsk)

Used to show the next visible view. Unlike the behavior of the `showNextView(boolean)` function, this function offers improved view switching functionality by skipping any hidden views during the transition. The parameter is a boolean value that determines whether to loop from the end back to the start.

##### Støttede komponenter

Beholder

##### Eksempel

```JavaScript
beholder1.showNextVisibleViewing (false)
```

#### PreviousView()

Used to show the previous view. The parameter is a boolean value that determines whether to loop from the start back to the end.

##### Støttede komponenter

Beholder

##### Eksempel

```JavaScript
container1.showPreviousView(sann)
```

#### PreviousVisibleView()

Used to show the previous visible view. Unlike the behavior of the `showPreviousView(boolean)` function, this function offers improved view switching functionality by skipping any hidden views during the transition. The parameter is a boolean value that determines whether to loop from the start back to the end.

##### Støttede komponenter

Beholder

##### Eksempel

```JavaScript
container1.showPreviousVisibleView(true)
```

#### reset()

Brukes til å tilbakestille verdien til standardverdien.

##### Støttede komponenter

Skjema

##### Eksempel

```JavaScript
form1.reset()
```

#### resetValue()

Brukes til å tilbakestille verdien til standardverdien.

##### Støttede komponenter

Skritt

##### Eksempel

```JavaScript
steps1.resetValue()
```

#### openModal()

Brukes til å åpne en modal

##### Støttede komponenter

Modell

##### Eksempel

```JavaScript
modal1.openModal()
```

#### closeModal()

Brukes til å lukke en modal

##### Støttede komponenter

Modell

##### Eksempel

```JavaScript
modal1.closeModal()
```

#### BildeUrl(streng)

Used to set the Image source URL. It can be a Base64 or a URL pointing to an image on the web.

##### Støttede komponenter

Bilde

##### Eksempel

```JavaScript
image1.setImageUrl("https://cloud-api.illacloud.com/drive/f/83095b77-1180-487e-86db-ff117e6083e5")
```

```JavaScript
image1.setImageUrl("data:image/png;base64,iVBORw0KGgoAA...II=")
```

#### clearReplyMessage()

Brukes til å fjerne den siterte svarmeldingen over inntastingsboksen.

##### Støttede komponenter

Samtale

##### Eksempel

```JavaScript
chat1.clearReplyMessage()
```

#### setPrimaryValue(nummer)

Brukes til å angi den primære verdien av statistikk.

##### Støttede komponenter

Statistikk

##### Eksempel

```JavaScript
Statistikk1.setPrimaryValue(100)
```

#### resetPrimaryValue()

Brukes til å nullstille primærverdien til standard primærverdi av statistikk.

##### Støttede komponenter

Statistikk

##### Eksempel

```JavaScript
statistisk1.resetPrimaryValue()
```

#### setFileUrl(streng)

Used to set the file URL. It can be a Base64 or a URL pointing to a PDF on the web.

##### Støttede komponenter

Pdf

##### Eksempel

```JavaScript
pdf1.setFileUrl("https://upload.wikimedia.org/wikipedia/commons/e/ee/Guideline_No._GD-Ed-2214_Marman_Clamp_Systems_Design_Guidelines.pdf")
```

#### play()

Brukes til å spille av video eller lyd.

##### Støttede komponenter

Video, lyd

##### Eksempel

```JavaScript
video1.play()
```

#### pause()

Brukes til å pause video eller lyd.

##### Støttede komponenter

Video, lyd

##### Eksempel

```JavaScript
video1.pause()
```

#### visning(boolsk)

Brukes til å angi om kontrollene skal vises.

##### Støttede komponenter

Video

##### Eksempel

```JavaScript
video1.showControls(sann)
```

#### VideoUrl(streng)

Used to set the video's URL. It can be a Base64 or a video URL on media websites.

##### Støttede komponenter

Video

##### Eksempel

```JavaScript
video1.setVideoUrl("https://youtu.be/8sUovZlBh_M")
```

#### setVolum(nummer)

Used to set the volume. The parameter is a number value between 0 to 1.

##### Støttede komponenter

Video, lyd

##### Eksempel

`video1.setVolume(0.5)`

#### setSpeed(nummer)

Used to set the speed. The parameter is a number value between 0.25 to 2.

##### Støttede komponenter

Video, lyd

##### Eksempel

```JavaScript
video1.setSpeedad (0.5)
```

#### Loop(boolsk)

Brukes til å angi om avspillingen skal gjentas.

##### Støttede komponenter

Video, lyd

##### Eksempel

```JavaScript
video1.setLoop(falske)
```

#### seekTo(nummer)

Used to seek to a specific time position. The parameter is a number value, measured in seconds (s). For example, to seek to the time position 5:35, you would use the value 335.

##### Støttede komponenter

Video, lyd

##### Eksempel

```JavaScript
video1.seekTo(335)
```

#### mute(boolean)

Brukes til å sette demping eller ikke.

##### Støttede komponenter

Video, lyd

##### Eksempel

```JavaScript
video1.mute(sann)
```

#### setAudioUrl(streng)

Used to set the audio's URL. It can be a Base64 or a video URL on media websites.

##### Støttede komponenter

Lyd

##### Eksempel

```JavaScript
video1.setAudioUrl("https://www.soundhelix.com/examples/mp3/SoundSong-1.mp3")
```

#### slickNext()

Brukes til å bytte til neste bilde.

##### Støttede komponenter

Carousel

##### Eksempel

```JavaScript
carousel1.slickNext()
```

#### slickPrevious()

Brukes til å bytte til forrige bilde.

##### Støttede komponenter

Carousel

##### Eksempel

```JavaScript
karusel1.slickPrevious()
```

#### setSrc()

Brukes til å sette kildens URL til IFrame.

##### Støttede komponenter

IFrame

##### Eksempel

```JavaScript
iframe1.setSrc("https://www.nasa.gov/")
```

#### setMarkers(matrise)

Used to set the markers on the map. The parameter should be an array of objects. The latitude and longitude fields in each object should be same as the fields set in the right-hand panel

##### Støttede komponenter

Kart

##### Eksempel

```JavaScript
map1.setMarkers([
{
    "lat":57.14009563040699,
    "lng":-114.6018657894011
 },{
     "lat":55.40432513713809,
     "lng":-112.98252263795787
 },{
     "lat":57.841288754888545,
     "lng":-103.3518657894011


```

#### resetMarkers()

Brukes til å nullstille markørene til standard markører.

##### Støttede komponenter

Kart

##### Eksempel

```JavaScript
map1.resetMarkers()
```

## Gå til adressen

### Struktur

```JavaScript
utils.goToURL({
url:string,
newTab:boolean
})
```

### Beskrivelse

Use an object to set it. There are 2 attributes in the object. The first attribute is `url`. Its value is a complete URL, including 'https://' or 'http://', and it is of type string. The second attribute is `newTab`. It is a boolean used to set whether to open the URL in a new tab.

### Eksempel

```JavaScript
utils.goToURL({
url:"https://www.illacloud.com",
newTab:true
})
```

## Kopier til utklippstavlen

### Struktur

```JavaScript
utils.copyToClipboard(streng)
```

### Beskrivelse

Brukes til å kopiere en streng

### Eksempel

```JavaScript
bruker copyToClipboard("The text to copy")
```

## Angi router

### Struktur

```JavaScript
utils.setRouter({
pagePath:string;
viewPath:string
})
```

### Beskrivelse

Used to change the page in ILLA Apps. There are 2 attributes in the object.

The first attribute is `pagePath`. The value of pagePath is shown on the following screenshot without `/`.

!\[](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=ZTkwODAzZmEzYmNlMDhlZDlmYjYzMDA4NjFlYIyMGJfWHE5WUtskplZnF1d05FaVuUW5IcnN6cVtbbNWcjdfV9rZrZW46RjZUam1I1a0b8bj0dVW54F2UnV2ZTZZZZZZZOmTTZOwm21OwmT&TKMG2MGGGGG_T&MXTMXTMXTKMXTMXTMXTMXT&CWMXTXTM

The second attribute is `viewPath`. The value of viewPath is shown on the following screenshot without `/`.

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=MzljM2ZkMDExYmE1NmRmNTQwNDZjNmU2NmM4YmQ1NzBfRTdCUFd2T3MwOXVpHNkxsUHZwdzFib29yMMMJtVXfV9rZW46Vld2J2JxD6B61B1IlxsUlXNHHHKUrTISEXNNNNN_G_G_G_G_G_G_TISEXOWMvS_D_D_TUMXOWMXOQ2TIS)

### Eksempel

```JavaScript
utils.setRouter({
pagePath:"page2", 
viewPath:"sub-page1"
})
```

## Lagre til ILLA-stasjon

### Struktur

```JavaScript
utils.saveToILLADrive({
fileName:string,
fileData:string,
fileType:string,
mappe:string,
tillat: Anonymous:boolean,
erstatte:boolsk
})
```

### Beskrivelse

Brukes til å lagre en fil til ILLA-driveren.

`fileName`: Navn med filutvidelse etter opplasting til ILLA-Drive.

`fileData`: Base64 eller rådata som JSON, CSV, klartekst, osv.

`fileType`: One of the following values: Auto, Plain text, JPEG, PNG, SVG, JSON, CSV, TSV, Excel(.xlsx). When using "Auto," we will attempt to determine the file type based on either the file extension or the file data.

`folder`: Destination folder for file upload. Leave it blank to upload to the root folder.

`allowAnonymous`: Used to set whether anonymous user access is allowed. If you plan to set your application as public, anonymous access must be enabled. For the security of your data, files or folders accessible anonymously must be placed in the anonymous folder.

!\[](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=Y2U5MzRmOGMxYWE4YzI2MjQxMjZhOWQ3FjODNDNkMjlfTDJkSzzg0QktPUmlQa3VZcxYBsZmVHVVVVHVhazlXS1ZfVG9rZW46SnyQ2aGbps0s0dZZZZZZZZZOwzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzvatr&vwwwwwwwwwwwwwwwwwwwwwwww

`erstatt`: Brukes til å sette når det finnes filer med samme navn i samme mappe, erstatte den eksisterende filen med ny fil, eller automatisk gi nytt navn til den nye filen.

### Eksempel

```JavaScript
//use upload1 for å velge filer fra enhet og laste opp til app-mappen. 
utils.saveToILLADrive({
fileName:upload1.files[0].name,
fileData:upload1. iles[0].dataURI,
fileType:"PNG",
mappe:"apps",
allowAnonymous:false,
replace:true
})
```

```JavaScript
//Last opp en tekstfil.  
utils.saveToILLADrive({
fileName:"test.txt",
fileData:"abc",
fileType:"Plain text",
mappe:"apps",
tillater Anonymous:false,
replace:true
})
```

## Last ned fra ILLA-stasjon

### Struktur

```JavaScript
utils.downloadFromILLADrive({
    downloadInfo:[{
        tinyURL:string,
        fileID:string
        }],
    asZip:boolean
})
```

### Beskrivelse

Brukes til å laste ned filer fra ILLA-driveren.

`downloadInfo`: an array of objects including tinyURL and fileID. You can use the component Drive file picker to pick files from ILLA Drive and get the `downloadInfo` via drivePicker1.value

`asZip`: brukes til å angi om filene skal lastes ned som en Zip.

### Eksempel

```JavaScript
utils.downloadFromILLADrive({
    downloadInfo:drivePicker1.value,
    asZip:true
})
```

## Nedlasting

### Struktur

```JavaScript
utils.downloadFile({
    fileType:string,
    fileName:string,
    data:anyType
})
```

### Beskrivelse

Brukes til å eksportere data til enheten din.

`fileType`: One of the following values: Auto, Plain text, JPEG, PNG, SVG, JSON, CSV, TSV, Excel(.xlsx). When using "Auto," we will attempt to determine the file type based on either the file extension or the file data.

`fileName`: et navn med utvidelse.

"data": data i filen.

### Eksempel

```JavaScript
//Last ned tabelldataene som en CSV
utils.downloadFile({
    fileType:"CSV",
    fileName:"text.csv",
    data: table2.dataSourceJS
})
```

```JavaScript
//download image
utils.downloadFile({
    fileType:"PNG",
    fileName:"text.png",
    data: image1.imageSrc
})
```

## Angi globale data

### Sett global data-verdi

#### Struktur

```JavaScript
utils.setGlobalDataValue({
key:string, 
value:any
})
```

#### Beskrivelse

Brukes til å angi verdien av gobalData

`nøkkel`: displayName of globalData as the following screenshot shows.

![](https://illasoft.larksuite.com/space/api/box/stream/download/asynccode/?code=NGE2OTMxZGFlYjE1ZmYwMGZlOWQ3MWE2ZDFiNTQ3ZmFfMDRUUWxYN3JxNndSZXdFdzI2blFZeDNUVFozRnRCYnVfV9rZrZW46TUc0YmJcjbzdXDJYlBREV4Y4YRBCRBCRTUMGG_TTTTTUMXOMXOMPDTKL)

"verdi" : verdien som skal settes

#### Eksempel

```JavaScript
//use input5 for å få brukerens input verdi og satt som verdi av stringState. 
utils.setGlobalDataValue({
key:"stringState", 
verdi: input5.value
})
```

### Angi globale data i

#### Struktur

```JavaScript
utils.setGlobalDataIn({
key:string, 
path:string, 
value:any
})
```

#### Beskrivelse

Når de globale dataene er en liste eller et objekt, bruk setGlobalDataIn for å endre verdien som tilsvarer en bestemt indeks i en liste eller for å endre verdien som tilsvarer en bestemt nøkkel i et objekt.

`key`: displayName of globalData.

`path`:  the index of the value you intend to update in the array or the key in the object. For example, to set the second value in array, the path is 1 (the index). To set the value of the attribute named key1, the path is key1.

"verdi" : verdien som skal settes

#### Eksempel

```JavaScript
//objectState er et objekt: {"key1":"value1", "key2":"value2"}, for å sette 
utils.setGlobalDataIn({
key:"objectState", 
sti:"key2", 
verdi:7.verdien
})
```

# Innebygde biblioteker

| **Bibliotek**      | \*\*Dokumenter                                                         | **Hvordan bruke den i ILLA-bygger**   | **Eksempel**                                                |
| ------------------ | ---------------------------------------------------------------------- | ------------------------------------- | ----------------------------------------------------------- |
| Lodaske            | https://lodash.com/docs/4.17.15                                        | `_`                                   | `_.concat(array, 2, [3], [[4]]);`                           |
| Dag.js             | https://day.js.org/                                                    | Dagjs\`                               | `dayjs(new Date())`                                         |
| uuid               | <https://www.npmjs.com/package/uid>                                    | `uid`                                 | `uuid.parse()`                                              |
| numbro             | https://numbrojs.com/                                                  | `numerikk`                            | `numbro(1000).format ({thousandSeparated: true})`           |
| Papa               | https://www.papaparse.com/docs                                         | `Papa`                                | `Papa.parse(url, {download: true, // rest of config ... })` |

# Verdi av handlinger, komponenter...

Use `displayName.xxx` to use the value of actions, components, or other globalData. Please note that in the Run Script, these variables can be directly accessed without using `{{` references.

```JavaScript
utils.downloadFromILLADrive({
    downloadInfo:{{drivePicker1.value}},//❌ 
    asZip:true
})

utils.downloadFromILLADrive({
    downloadInfo:drivePicker1.value,/:check_mark_button:
    asZip:true

```
